# 卒業研究：バス混雑度回答アプリのUI／ユーザビリティ調査 仕様書（v0.1 ドラフト）

## 0. 研究の位置づけ
- **元の目的**：バス車内人数の正解データを収集し、BLEによる人数推定に教師ラベルを与える（＝院進後の本研究）。
- **卒業研究**：そのアプリ自体を題材に、**「どの回答UIが速く・正確に・負担少なく答えられるか」**を調べる。
- **接続**：回答UIの良し悪しは、そのまま**教師データの量と質**を左右する。卒研の結果が本研究の基盤になる。

---

## 1. リサーチクエスチョン（RQ）
- **RQ1**：人数入力の**初期値・操作系**（未入力／最小値／中央値／最大値スタート、ステップ幅）は、
  **回答所要時間・タップ数・訂正回数**にどう影響するか。
- **RQ2**：**車内の混雑度**と**回答所要時間**にどのような関係があるか（混雑時は遅くなる／雑になるのか）。
- **RQ3**：経路への**慣れ**（普段使い路線 vs 不慣れな路線）は、**バス停入力**の時間・失敗率にどう影響するか。
- **RQ4**：**バス停入力方式**（自動補完／履歴・お気に入り優先／地図選択）の比較。

---

## 2. 実験デザイン
- **被験者内計画（within-subject）**：1人が複数条件を経験する（個人差の影響を抑えられる）。
- **条件の割当**：**1回答（＝1セッション）ごと**にアプリが自動で条件を切替。順序効果を避けるため
  ランダム化またはラテン方格でカウンターバランス。
- **被験者は条件を意識しない**（アプリが裏で割当・記録）。
- 補足：条件を増やすと必要サンプルが急増するので、**まずRQ1（人数入力）を主軸**にし、他は固定または2条件に絞る。

---

## 3. 独立変数（UIの条件）
| 記号 | 条件 | 水準 |
|---|---|---|
| **A** | 人数ステッパーの**初期値** | `none`(—/未入力) / `min`(範囲下限) / `median`(中央値) / `max`(上限) / `zero`(0) |
| **B** | **ステップ幅** | ±1のみ / ±1と±5 / ±1と±10 |
| **C** | **バス停入力方式** | 自動補完 / 履歴・お気に入り優先 / 地図選択 |
| **D** | **方向トグル**（往路・復路を矢印で切替） | あり / なし |

> 現行アプリは A=`none`, B=クラス範囲に応じ±5か±10, C=自動補完, D=なし。

---

## 4. 従属変数（測定する指標）
**時間**
- `t_total`：報告画面表示 → 送信完了
- `t_class`：報告画面表示 → クラスをタップ
- `t_count`：人数画面表示 → 送信／スキップ
- `t_stop`：バス停欄フォーカス → 候補確定

**操作コスト**
- `taps_total`（総タップ数）、`taps_stepper`（±ボタン押下回数）
- `corrections`（戻る・選択やり直し・クリアの回数）
- `stop_chars`（バス停で入力した文字数）、`stop_rank`（選んだ候補の順位）、候補スクロール量

**結果・行動**
- 送信完了率／離脱率、スキップ率、バス停入力率、位置情報許可率
- 未選択のまま送信しようとした回数（入力エラー）

**主観（任意・短く）**
- 使いやすさ5段階、SUS短縮版など。回答負担を増やさないため**数回に1度**だけ表示。

---

## 5. ★中核：ログ仕様（アプリに仕込むもの）
**1回答＝1セッション**として、操作イベントを時系列で記録する。

```json
{
  "session_id": "uuid",
  "started_at": "2026-09-03T10:12:33.120Z",
  "variant": { "count_init": "median", "step": "1/10", "stop_ui": "autocomplete", "dir_toggle": false },
  "context": {
    "congestion_class": 4,
    "exact_count": 31,
    "familiar_route": true,
    "board_stop": "学園前駅（北）",
    "dest_stop": "高山サイエンスタウン",
    "gps_permission": "granted",
    "app_version": "phaseA-1.3"
  },
  "events": [
    { "dt": 0,     "type": "screen_view",   "screen": "picker" },
    { "dt": 1830,  "type": "tap",           "target": "class_4" },
    { "dt": 1840,  "type": "screen_view",   "screen": "count" },
    { "dt": 2600,  "type": "stepper",       "target": "+1",  "value": 27 },
    { "dt": 3120,  "type": "stepper",       "target": "+10", "value": 37 },
    { "dt": 4400,  "type": "stop_focus",    "field": "board" },
    { "dt": 5200,  "type": "stop_input",    "field": "board", "chars": 3 },
    { "dt": 5600,  "type": "stop_candidates","field": "board", "count": 12 },
    { "dt": 6100,  "type": "stop_select",   "field": "board", "rank": 2 },
    { "dt": 8400,  "type": "tap",           "target": "send" },
    { "dt": 8410,  "type": "submit",        "duration_ms": 8410, "taps": 7 }
  ],
  "summary": { "t_total": 8410, "t_class": 1830, "t_count": 6570, "t_stop": 1700,
               "taps_total": 7, "taps_stepper": 2, "corrections": 0 }
}
```

**イベント種別（最小セット）**
`session_start` / `screen_view` / `tap` / `stepper` / `stop_focus` / `stop_input` /
`stop_candidates` / `stop_select` / `stop_clear` / `back` / `skip` / `submit` / `gps_status` / `session_end`

**設計方針**
- `dt` は**セッション開始からの相対ミリ秒**（端末時計のズレに影響されない）。
- 個人を特定する情報は入れない（`surveyor_id` のみ）。
- 端末情報は**画面幅・OS種別程度**に留める。

---

## 6. 保存とエクスポート
- **localStorage に蓄積**（オフラインのバス車内でも欠落しない）。
- 送信可能になったら **Supabase の別テーブル `ui_logs`** に送る（既存 `responses` とは分離）。
- 分析用に **JSONで一括エクスポート**（`json_agg`）→ Python(pandas) で解析。

---

## 7. 分析方法
- JSON → pandas で整形（1行=1セッション、イベントは別テーブル化）。
- 記述統計（条件別の所要時間・タップ数の分布、中央値・四分位）。
- 条件間比較：**対応あり**なので Wilcoxon の符号順位検定／反復測定ANOVA、
  個人差を扱うなら**混合効果モデル**（被験者をランダム効果）。
- RQ2：混雑度クラス別の `t_total` 分布、相関・回帰。
- 可視化：箱ひげ図、条件×時間、学習曲線（回数を重ねると速くなるか）。

---

## 8. 倫理・同意
- 参加は任意、いつでも中止可。匿名で分析。
- **操作ログを取ること**を同意文に明記（現READMEの収集項目に追記が必要）。
- 位置情報・人数・バス停は引き続き**すべて任意**。

---

## 9. 実装ステップ（アプリ改修）
1. **ログ計装の土台**：イベント記録・セッション管理・summary算出
2. **条件割当（バリアント）**：セッションごとに条件を決めて記録、UIを切替
3. **保存・送信**：localStorage → `ui_logs` テーブル、JSONエクスポート
4. **付帯機能**：登録時に「普段使いのバス停・路線」を設定（RQ3の`familiar_route`判定に使う）、
   往路/復路の方向トグル
5. **分析スクリプト**：JSON→pandas→図表

---

## 10. 要決定事項（ユーザー判断が必要）
1. **被験者**：何人・どう募集するか（研究室／友人／授業など）。目安：被験者内なら10〜20人×複数回。
2. **実施環境**：**実バス乗車**で取るか、**模擬（教室で写真/動画を見て回答）**か。
   - 実バス＝生態学的妥当性は高いが混雑度を統制できない
   - 模擬＝混雑度を統制でき条件比較しやすいが現実味が落ちる
   - **併用（模擬でRQ1、実バスでRQ2）**が現実的
3. **「混雑度」の定義**：BLE正解が無いので、**回答されたクラス（主観）**を使う想定でよいか。
4. **条件をどこまで増やすか**：まずAのみ（5水準→3水準に絞る等）か、A×Cまでやるか。
5. **普段使い設定**を登録時に入れるか（RQ3に必要）。
6. 主観評価アンケートを入れるか（頻度は？）。
