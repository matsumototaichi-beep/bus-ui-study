# 卒業研究：移動中の人数回答UIに関する実験計画（v1.0）

> v0.1（ラボ実験前提・RQ4方式比較あり）から全面改訂。
> 実施環境が**実バス**、被験者が**募集した志願者**に確定したことによる。
> **実施の段取り（場所・人数・日数・費用・基準値の作り方）は `EXPERIMENT_PLAN.md` を参照**（2026-09-12〜）。

---

## 0. 研究の位置づけと最終目的

- **もともとの目的**：バス車内人数の正解データを集め、BLEによる人数推定に教師ラベルを与える（＝**進学後の本研究**）。
- **卒業研究**：そのアプリ自体を題材に、**回答UIの設計が「集まるデータの量と質」をどう左右するか**を実証する。

**最終目的（1文）**

> 移動中の市民センシングにおいて、回答UIの設計が「集まるデータの量と質」をどう左右するかを実証し、
> 状況（混雑）に応じた回答UIの設計指針を示す。

**本研究との接続**：BLE推定は教師ラベルの質がすべてである。歪んだラベルで学習したモデルは歪む。
卒研で「どう聞けば歪まないデータが取れるか」を先に解明することで、院の本研究は
**前提が定量的に保証された状態**で始められる。接続点は「正解データの信頼性」の一点であり、
BLEの推定精度そのものには卒研は貢献しない（これは正しく認識しておく）。

**書き方の注意**：論文は「バスアプリの話」ではなく
**「移動中に数を報告させるUI一般の話」**として書く。バスは題材であって主題ではない。

---

## 1. 中心的な主張（仮説）

> **H1**：回答UIが提示する初期値は、回答値そのものを初期値の方向に引きつける（アンカリング効果）。
> **H2**：その引きつけは**入力方式によって強さが変わる**（ステッパー＞テンキー）。
> **H3**：その引きつけは**回答が難しい状況（混雑時）ほど強くなる**。
>
> ⇒ 系：**最もデータが欲しい混雑時に、最もデータが歪む。**

### アンカリング効果とは
最初に提示された数値が判断の基準点になり、そこから十分に離れられない認知バイアス。
Tversky & Kahneman (1974), *Science*, 185(4157), 1124–1131.

本研究での現れ方：人数欄に `33` が入っていると、実際は40人でも「35」あたりで止めてしまう。
`—`（未入力）なら自分で数えるしかない。

---

## 2. 測れるもの／測れないもの ★重要

実バスでは**誰も真の乗車人数を知らない**。実験者が同乗して数える案は
「実験者自身が間違える可能性がある」ため採用しない。補助的なラボ実験も実施しない。

| | 測れるか | 根拠 |
|---|---|---|
| **正確さ（accuracy）** | ✗ **測れない** | 正解が存在しない |
| **系統的バイアス** | ✓ 測れる | 条件はアプリが無作為割当。条件間で状況の平均は等しいはずなので、**回答分布のズレの原因はUI以外にない** |
| **信頼性（一致度）** | ✓ 測れる | 同一便に複数名が乗れば、回答のばらつきが測れる（評価者間信頼性 / 級内相関 ICC） |
| **速さ・操作量・欠損** | ✓ 測れる | ログから直接 |

**論文での言い方**：「どちらが正確か」ではなく
**「どちらが歪みにくく、人によるばらつきが小さいか」**と書く。これで主張は成立する。

> 補足：事後アンケートの「実際に数えましたか／表示された数字に影響されたと思いますか」が、
> 正解の代わりに**主観側からアンカリングを裏付ける**役割を果たす（§8）。

---

## 3. 実験デザイン

**被験者内計画（within-subject）／実バス／1路線に固定（大阪か奈良かは未定）**

- 路線を1つに固定する（実験環境を変えないことを優先。路線間比較は次のステップ）
- 1回の実験で**複数名が同じ便に乗る**（→ 同一便での回答の一致度が測れる）
- 便IDは事業者から取得できないため、**実験者が「実験コード」を配る**（例：4桁）。
  参加者はアプリに入力するだけで、以降の全セッションに記録される。**便が変わるたびに新しいコードを配る**（日付が変わると自動で未設定に戻る）

### 独立変数（2要因 × 2水準 ＝ 4条件）

| 記号 | 要因 | 水準 |
|---|---|---|
| **A** | 入力方式 | `stepper`（−10/−5/−1 [値] +1/+5/+10） ／ `numpad`（自前テンキー） |
| **B** | 初期値 | `none`（未入力 `—`） ／ `median`（選択クラスの目安中央値をプリセット） |

### 準独立変数
- **C：混雑度** — 統制できないので自然変動を事後に層別する

### 条件の割当（実装済み）
```
UI_CONDS = [ stepper/none, numpad/median, stepper/median, numpad/none ]
```
- 1回答＝1セッションごとに次の条件へ自動で進む（連続で同じ入力方式が続かない順）
- `condOffset()` が**端末ごとにランダムな開始位置**を保存 → 被験者間で順序が揃わないようにし、
  順序効果と条件効果を分離する
- 被験者は条件が切り替わっていることを知らされない

### なぜ OS のキーボードを使わないか
テンキーをOS標準のソフトキーボードで実装すると、**表示遅延と画面占有**が交絡し、
「テンキーが遅かったのは入力方式のせいではなくキーボード待ちのせい」と反論されて終わる。
そこで**自前のテンキーを画面内に固定表示**する。副次的な利点：
ボタンサイズを統制できる／タップ座標が記録できる。

### 送信ボタンの固定
テンキーは縦に長く、そのままでは送信ボタンが画面外になる。
入力方式によって**送信への到達コストが変わる**のを避けるため、
送信／スキップ行をスクロール領域の下端に固定した（両条件に等しく適用）。

---

## 4. 被験者と規模

| | 値 | 備考 |
|---|---|---|
| 被験者数 | **最低10名**／理想20〜30名 | 指導教員より「10名は必須」 |
| 1人あたり回答数 | **15〜18回答**（各条件4回以上） | 1乗車で複数回答（バス停ごと）して稼ぐ |
| 想定 | 1人2〜3乗車 × 各5〜6回答 | |
| 総セッション数 | 10名で **150〜180** | 1条件あたり約40 |

- 被験者内計画は個人差を除去できるため、被験者間計画より必要人数が大幅に少ない
- 正確な検出力分析は効果量の仮定が必要。**まず1〜2名でパイロットを回してから確定**する

---

## 5. 従属変数

### 時間
| 記号 | 定義 | 状況 |
|---|---|---|
| `t_total` | セッション全体 | ✅ |
| `t_class` | 報告画面表示 → クラスタップ | ✅ |
| `t_count` | 人数画面表示 → 送信/スキップ | ✅ |
| `t_first` | 人数画面表示 → 最初の値入力操作（**思考時間と操作時間の分離**） | ✅ |
| `t_stop` | バス停欄の滞在時間 | ✅（乗車/降車の分離は➕） |

### 操作コスト
| 項目 | 意味 | 状況 |
|---|---|---|
| `taps_total` / `taps_value` | 総タップ数／値を作るための操作数 | ✅ |
| ±1/±5/±10 の使用比率 | どの粒度が実際に使われたか | 🔧 |
| **オーバーシュート回数** | ＋の後に−へ方向転換した回数＝**押しにくさの客観指標** | 🔧 |
| テンキーの無効タップ数 | 3桁目を押した回数（`ignored:true`） | ✅ |
| `corrections` | 戻る／クリア／選び直し | ✅ |
| バス停：入力文字数・選んだ候補の順位・候補を出したが選ばなかった率 | 予測変換の当たり具合 | ✅ |

### 回答内容（★中心）
| 項目 | 何が言えるか | 状況 |
|---|---|---|
| **回答値 − 初期値**（符号付き） | **アンカリングの大きさと向き** | 🔧 |
| **クラス選択と入力人数の乖離** | 目安範囲外に入力した割合と方向 | 🔧 |
| **クラス別の乖離率** | どのクラス境界が曖昧か＝**クラス設計の妥当性** | 🔧 |
| 丸め傾向（5・10の倍数への集中） | 実数え上げか概算か | 🔧 |
| **同一便の被験者間ばらつき** | 信頼性（ICC）。正解なしで測れる | ✅ `exp_code` |
| 回答値の分布形状 | UIによる歪み | 🔧 |

### 完遂・欠損
送信完了率／離脱率（`abandon`）／スキップ率／バス停入力率（乗車・降車別）／位置情報許可率 — すべて ✅

### 文脈・共変量
| 項目 | 状況 |
|---|---|
| 混雑度（クラス） | ✅ |
| **着席／立席** ← 片手操作かを左右する最大の交絡要因 | ✅ `posture` |
| 時間帯・曜日 | ✅ |
| 実験コード（＝便の識別） | ✅ `exp_code` |
| 端末サイズ | ✅ |
| 何回目のセッションか（学習効果） | ✅ `variant.seq` |
| 条件の開始位置 | ✅ `variant.offset` |

凡例：✅実装済み ／ 🔧既存ログから計算可能 ／ ➕要実装

---

## 6. ログ仕様（実装済み）

1回答＝1セッション。時刻は `performance.now()` 起点の相対 ms（`dt`）で記録し、端末時計のズレを回避する。

```json
{
  "session_id": "uuid",
  "started_at": "2026-09-10T10:12:33.120Z",
  "outcome": "submit | skip | abandon",
  "variant": { "input": "numpad", "count_init": "median", "seq": 7, "offset": 2 },
  "context": {
    "congestion_class": 4, "exact_count": 31, "count_touched": true,
    "board_stop": "...", "dest_stop": "...", "gps_points": 12,
    "screen_w": 375, "screen_h": 667, "lang": "ja", "app_version": "uiStudy-0.5",
    "exp_code": "0312", "posture": "stand", "response_cid": "uuid"
  },
  "summary": { "t_total": 18420, "t_class": 3110, "t_count": 15300, "t_first": 2100, "t_stop": 5200,
               "taps_total": 9, "taps_value": 5, "corrections": 1 },
  "events": [
    { "dt": 0,    "type": "screen_view", "screen": "count", "input": "numpad", "count_init": "median" },
    { "dt": 1240, "type": "numkey", "key": "3", "value": 3 },
    { "dt": 1880, "type": "numkey", "key": "8", "value": 38 },
    { "dt": 3010, "type": "tap", "target": "send" },
    { "dt": 3010, "type": "session_end", "outcome": "submit" }
  ]
}
```

イベント種別：`session_start` `screen_view` `tap` `stepper` `numkey`
`stop_focus` `stop_input` `stop_candidates` `stop_select` `stop_clear` `session_end`

保存先：localStorage → Supabase `ui_sessions`（**テーブル未作成**。§10）

---

## 7. 分析計画

1. **前処理** — JSON エクスポート → pandas。異常セッション（極端に長い＝中断）を規準を決めて除外
2. **主分析** — 混合効果モデル（被験者をランダム効果）
   - 目的変数：回答値 −（クラス目安中央値）＝ 正規化した回答のズレ
   - 説明変数：入力方式 × 初期値 × 混雑度
   - **交互作用が中心的な主張**（H2・H3）
3. **時間・操作量** — 同じモデル構造で `t_count`・`taps_value` を目的変数に
4. **信頼性** — 同一実験コード内の回答について級内相関 ICC を条件別に算出
5. **クラス設計の妥当性** — クラス選択と入力人数の乖離をクラス別に集計
6. **主観との突合** — アンケート得点と客観指標の一致／不一致（§8）

---

## 8. 事後アンケート

詳細は **`QUESTIONNAIRE.md`**。狙いは3つ。

1. **条件別の主観評価** — 4つのUIを再提示して評価させる（実験中は条件が混ざるので記憶に頼れない）
2. **回答行動の自己報告** — 「実際に数えたか／目測か」。ログでは分からない部分を埋める
3. **アンカリングの自覚** — 「表示されていた数字に影響されたと思うか」。
   **気づいていないのに客観データでは影響が出ている**なら、アンカリングの証拠として強い

---

## 9. 副次的に書ける章（追加実験なしで出せる）

| | 内容 |
|---|---|
| 副1 | クラス設計の妥当性（同一便での一致度、クラスと人数の乖離） |
| 副2 | 混雑度と回答負担（所要時間・スキップ率・ばらつき）＝**欲しい時ほど取れないジレンマ** |
| 副3 | バス停予測変換の実態（入力文字数・候補順位・大阪の同名/類似名問題） |
| 副4 | ステップ幅の使われ方（±1/±5/±10の使用比率・オーバーシュート） |

---

## 10. 実装状況と残タスク

### 完了
- 人数入力のクラス制限撤廃（クラス範囲は「目安」と初回タップの起点のみ）
- ステッパー `−10 −5 −1 [値] +1 +5 +10`、値は常時枠表示（未入力＝破線／入力後＝青実線）
- **自前テンキー**と入力方式の条件切替、4条件のローテーション、端末別オフセット
- 送信/スキップの下端固定
- 操作ログ（イベント・所要時間・タップ数・訂正回数）、localStorage 保存、JSON エクスポート
- 小画面対応（iPhone SE2/SE3・SE1 で検証済み）
- **実験コード**（報告画面上部。日付が変わると自動で未設定に戻る＝前日のコードのまま別の便と誤って紐づくのを防ぐ）
- **着席／立席トグル**（報告画面上部。切り替えるまで保持。日付が変わると未設定）
- **`t_first`・`t_count`** の記録
- **結合キー**：回答（`ui_responses.session_id`）と操作ログ（`ui_sessions.session_id`）を突合できるようにした

### 残タスク（優先順）
1. **Supabase で `ui_responses.sql` と `ui_sessions.sql` を実行**（どちらも未実行。現状は端末の localStorage にしか残らない）
2. **実験版のホスティング**（リモート未設定。参加者のスマホから HTTPS で開ける URL が必要。位置情報は HTTPS 必須）
3. **バス停辞書**（地域・路線が確定してから）
   - 奈良の場合：既存の奈良県＋京都府 1236件（P11・2010年）を対象路線の停留所に絞る
   - 大阪の場合：国土数値情報 P11 大阪府(27) から再生成
   - いずれも2010年データのため**対象路線の停留所は事業者公式サイトで照合**する
4. 事後アンケートの実装（Google Forms。`QUESTIONNAIRE.md`）
5. 分析スクリプト（JSON → pandas）
6. パイロット実験（1〜2名）

---

## 11. 未決定事項

- 実施時期・実施日数、1人あたりの乗車回数
- **実施地域（大阪か奈良か）**と対象路線。奈良で行う場合は**進学先研究室の確認が必要**（海外出張から帰国後）
- 研究倫理審査の要否と申請時期 ← **リードタイムが長い。指導教員に早めに確認**
- バス事業者への事前連絡の要否
- 被験者の募集方法と謝礼
