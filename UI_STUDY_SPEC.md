# 卒業研究：移動中の人数回答UIに関する実験計画（v1.0）

> v0.1（ラボ実験前提・RQ4方式比較あり）から全面改訂。
> 実施環境が**実バス**、被験者が**募集した志願者**に確定したことによる。
> **実施の段取り（場所・人数・日数・費用・基準値の作り方）は `EXPERIMENT_PLAN.md` を参照**（2026-09-12〜）。

---

## 0. 研究の位置づけと最終目的

- **もともとの目的**：バス車内人数の正解データを集め、BLEによる人数推定に教師ラベルを与える（＝**進学後の本研究**）。
- **卒業研究**：そのアプリ自体を題材に、**回答UIの設計が「集まるデータの量と質」をどう左右するか**を実証する。

**最終目的（1文）**

> 移動中の市民センシングにおいて、回答UIの設計が「集まるデータの量と質」をどう左右するかを実証し、
> 状況（混雑）に応じた回答UIの設計指針を示す。

**本研究との接続**：BLE推定は教師ラベルの質がすべてである。歪んだラベルで学習したモデルは歪む。
卒研で「どう聞けば歪まないデータが取れるか」を先に解明することで、院の本研究は
**前提が定量的に保証された状態**で始められる。接続点は「正解データの信頼性」の一点であり、
BLEの推定精度そのものには卒研は貢献しない（これは正しく認識しておく）。

**書き方の注意**：論文は「バスアプリの話」ではなく
**「移動中に数を報告させるUI一般の話」**として書く。バスは題材であって主題ではない。

---

## 1. 研究の問いと仮説

> **★2026-09-25 全面改訂。** 指導教員の助言による。
> 旧版は「混雑時に数えられない」ことを仮説の中心（H2・H3）に据えていたが、
> それだと**数えられないまま終わる**。数えられる便を主戦場にし、
> 数えられない便では**人数の代わりになる指標を探す**、という2本立てにする。
>
> **路線は吹田市内線2系統のまま変えない。混雑水準は「便の時間帯」で作る**（2026-09-25 確定）。
> 当初は空いている路線と混んでいる路線の2路線構成を検討し、千里ニュータウン周辺の候補を
> 調査したが（`docs/log/2026-09-25.md`）、指導教員より「時間をずらせば今の路線でも空いている便が取れる」
> と助言があり、**同一路線・同一区間で時間帯だけを変える**方式に決めた。
> **路線・停留所・所要時間・運賃・のりばがすべて揃うので、混雑以外の条件差が入らない。**
> 交絡の観点では2路線構成より明確に優れている。

### 位置づけ（この研究が最終的に主張すること）

> **移動中のバス車内で、正確な乗車人数を測ることはできない。**
> **しかし「混んでいるかどうか」は取れる。そして混雑時には、人数の代わりになる手がかりがある。**

この境目は先行実験（2026-09-18）で実測された。車内14〜20人の便では終点で車内人数がちょうど
0に閉じ、アプリの回答とも一致した（＝**数えられた**）。一方39人まで増えた便では最大10人ずれ、
本人の記録にも「正直この辺から車内人数は適当」とある（＝**数えられなかった**）。

### 2つの便（同じ路線・同じ区間・時間帯だけが違う）

| | 空いている便 | 混んでいる便 |
|---|---|---|
| いつ | **15時台**（早い集合の回） | **17時台以降**（遅い集合の回） |
| 先行実験からの見込み | 5〜15人 | 20〜39人（17:18発は**実測39人**） |
| そこで起きること | 乗客は実際に数えきれる | 誰にも数えきれない |
| 計数役の基準値 | **正解として機能する** | 崩れる（参照値どまり） |
| 調べること | **どの回答UIが良いか**（RQ1） | **人数の代わりになる指標は何か**（RQ2） |

**混雑度は、統制できない交絡から、便の時間帯で作る操作変数（要因C）に格上げした。**
旧版の「統制できないので自然変動を事後に層別する準独立変数」という扱いは**廃止**する。

**割り付けは「日」単位で行う。** 同じ参加者に1日で空いている便と混んでいる便の両方を経験させると
拘束が3時間を超えるので、**日によって集合時刻を変える**（早い回の日／遅い回の日）。
参加者は別日にもう一度来てもらう形で、要因Cを被験者内にできる。
それが組めない参加者については被験者間でも可とする（2026-09-24 松本の判断）。

> ⚠️ **遅い回の日は、松本が17時台に集合場所にいる必要がある。** アルバイトとの兼ね合いを確認すること。
> 松本は同乗しないので、拘束は集合・説明・送り出しの約40分。

### RQ1（空いている便）数えられる状況で、どの回答UIが良いか

> **H1**：直接入力（テンキー）は逐次調整（ステッパー）より、**所要時間が短く操作量が少ない**。
> **H2**：**「わからない」という逃げ道を用意すると、人数の欠損が増える**。

### RQ2（混んでいる便）正確な人数の代わりになる指標は何か

誰も数えきれない状況で、車内人数の手がかりになるものを探す。

#### 第一候補は「クラス6段階」そのもの（2026-09-25 確定）

**まず既存の6段階クラスが指標として機能するかを検証し、そのうえで他の手がかりを探す。**
アプリの改修が一切要らないうえ、クラスは**1タップで答えられるので混雑時にも欠損しにくい**。
「正確な人数は測れないが、混んでいるかどうかは取れる」という本研究の位置づけに最も素直に対応する。

検証すべきこと：

| # | 問い | 見るもの |
|---|---|---|
| 1 | クラスと実人数の対応は単調か | 空いている便で、クラス回答 × 計数役の実測人数 |
| 2 | クラスの境界は実態に合っているか | 同上。§9 副1（クラス設計の妥当性）と同じ分析 |
| 3 | **混雑時にクラスの一致度は落ちるか** | 同一便に乗った複数名のクラス回答の一致度（級内相関 ICC）を、空いている便と混んでいる便で比較 |
| 4 | 人数が欠損してもクラスは残るか | 混んでいる便での「クラスのみ回答」の割合 |

**3 が要になる。** 人数は混雑時に崩れることが先行実験で分かっているが、
**クラスの一致度まで崩れるなら「混んでいるかどうかも取れない」ことになり、本研究の位置づけが成立しない。**
逆に一致度が保たれるなら、「人数は無理だがクラスは取れる」と実データで言える。

#### そのあとに探す候補

第一候補の検証と並行して、以下を事後分析で探索する。候補は2系統ある。

**(a) 回答行動から自動で取れるもの**（アプリの改修が不要。すでにログに記録している）

| 候補 | なぜ指標になりうるか | ログ上の場所 |
|---|---|---|
| 回答に要した時間 | 4人を超えると1人あたり約250〜350ms増えるという報告がある（Trick & Pylyshyn, 1994）。混むほど長くなる | `t_count` |
| 「わからない」率・未入力率 | 混むほど上がる | `count_unknown` / `exact_count is null` |
| 同一便内での回答のばらつき | 混むほど散る（級内相関 ICC の逆数的な使い方） | 同一便の `exact_count` 群 |
| 5・10の倍数への丸め率 | 数えていないほど丸める | `exact_count` の分布 |
| 入力の修正回数（オーバーシュート） | 数えあぐねるほど増える | `taps_value` |

(a) の強みは、**人数を聞かなくても混雑水準が推定できる**という主張になること。
BLE推定（§0）への接続として筋が通り、かつ**アプリの改修が一切要らない**。

**(b) 回答者に聞くもの**（アプリの改修が要る。**足すなら1項目まで**）

| 候補 | 理屈 |
|---|---|
| **立っている人の数** ◎ | 座っている人は死角に入るが、立っている人は視界に入る。**混むほど立ち客が増えるので、欲しいところで精度が上がる方向**。クラス3以上なら「座席数（約30）＋立ち客数」で総数を近似できる |
| 空席の数 | 数が小さいときに正確。ただし混雑時は0で飽和するので、立ち客数と相補的 |
| 自分の周囲の人数 | 全体を見渡さなくてよい。複数人の回答を合成すれば全体を推定できる可能性 |
| つり革が埋まっている／通路を移動できない | 離散的で判断が速い |
| 幅で答える（「20〜30人」） | 点推定を強制しない |

> ⚠️ **項目を足すと回答負担が増え、回答率そのものが下がる。回答率は本研究の従属変数なので、これは実害になる。**
> 足すのは1項目までとし、足した場合は「その項目の有無」自体を交絡として記述すること。

> **H3**：混雑が進むほど、**人数の欠損率・回答所要時間・同一便内での回答のばらつき・丸め率**が増える。
> したがってこれらは**混雑水準の代理指標として使える**。
> **H4**：**クラス6段階の回答は、人数と違って混雑時にも欠損せず、同一便内での一致度も保たれる。**
> したがってクラスは混雑時の代替指標として機能する。
>
> ⇒ 系：**最もデータが欲しい混雑時に、最も人数が得られない。だからこそ代理指標が要る。**

> **H4 は反証されうる形で立てている。** 混雑時にクラスの一致度まで落ちるなら H4 は棄却され、
> 「混んでいるかどうかも当てにならない」という結論になる。その場合は (b) の追加項目へ進む。

### RQ3 空いている便で作った関係は、混んでいる便に外挿できるか

空いている便では計数役の実測が正解として機能するので、「指標 → 総数」の関係式が引ける。
これを混んでいる便に当てはめて総数を推定し、**崩れながらも取れた実測とどれくらい整合するか**を見る。

> **限界**：空いている域で作った関係式を混んでいる域に外挿している。正しい保証はない（§8）。

### H2 が RQ1 の中心。これは「良くする」話ではなくトレードオフの話である

移動中のバス車内で正確な人数を数えるのは、実際にはほとんど不可能に近い
（先行実験での実験者本人の報告：「余程のことがない限り正確な人数を数えるのは不可能かも」）。
そのとき回答UIには2つの道しかない。

| | 「わからない」ボタンなし | 「わからない」ボタンあり |
|---|---|---|
| 数えられない人の行動 | **適当な数を入れて送る** | 「わからない」を選ぶ |
| 集まるデータ | 量は多いが**嘘のラベルが混ざる** | 量は減るが**残った値は信用できる** |

**「嘘のラベルが多いほうがいいのか、正直で少ないラベルがいいのか」** — これが本研究の問いである。
BLE推定の教師ラベルを集めるという最終目的（§0）に、この問いは直接効く。

この対立は机上の想定ではなく、**先行実験で実際に観測された**。
初期値が入った状態から「わからない」に戻す手段が無かったステッパー条件では、
数えられなかった回答が **0人** として3件記録された（`docs/log/2026-09-19_hankyu.md` §4）。
実験者本人の言葉では「数えられないのに適当な数でいいよとしてしまう」。

### ★2026-09-19 大幅改訂：アンカリング仮説を取り下げた

旧版の H1〜H3 は**初期値提示によるアンカリング効果**（Tversky & Kahneman, 1974）が中心だった。
これを取り下げた理由は2つある。

**(1) 「初期値なし」条件が、入力方式によって意味が違う。**
ステッパーの初回タップは0からではなく**クラスの下限から**始まる（タップ数削減のため）。
クラス5（46〜60人）なら、テンキーで `7` を押せば `7` だが、ステッパーで `+1` を押すと **47** になる。
つまり「初期値なし」でもステッパーには下限アンカーが効いている。
**ステッパーは起点なしには成立しない**ので、これは取り除ける交絡ではなく入力方式の性質そのものであり、
「アンカーあり／なし」の対比が成立しない。

**(2) 「わからない」に戻す手段が条件間で非対称だった**（上記の0人問題）。

代わりに「わからない」ボタンの有無を第2要因に据えた。
**どちらの水準もボタンを置くか置かないかの違いだけ**なので、入力方式による意味のズレが起きない。

> なお、6クラスは人数レンジそのものを画面に表示している（例「目安 46〜60人」）。
> **レンジによるアンカリングは全条件に共通して残る**ので、本研究はそれを操作していない。
> 事後アンケートの「表示された目安に影響されたと思いますか」（§8）で主観側から扱う。

---

## 2. 測れるもの／測れないもの ★重要

実バスでは**誰も真の乗車人数を知らない**。実験者が同乗して数える案は
「実験者自身が間違える可能性がある」ため採用しない。

| | 測れるか | 根拠 |
|---|---|---|
| **正確さ（accuracy）** | ✗ **測れない** | 正解が存在しない |
| **系統的バイアス** | ✓ 測れる | ★2026-09-13 訂正。旧版は「条件はアプリが無作為割当なので状況の平均は等しい」としていたが、**12名では偶然の釣り合いに頼るのは弱く、これでは根拠にならない**。正しい根拠は `EXPERIMENT_PLAN.md` §3.1 の**二重差分**＝①同じ人が同じ乗車の中で両条件を経験する（人の癖が消える）②同じ区間で参加者間の条件を反転させる（その瞬間の混雑が消える） |
| **信頼性（一致度）** | ✓ 測れる | 同一便に複数名が乗れば、回答のばらつきが測れる（評価者間信頼性 / 級内相関 ICC） |
| **速さ・操作量・欠損** | ✓ 測れる | ログから直接 |

**論文での言い方**：「どちらが正確か」ではなく
**「どちらが歪みにくく、人によるばらつきが小さいか」**と書く。これで主張は成立する。

> ★2026-09-19 改訂：アンカリング要因の廃止（§1）に伴い、旧注記（アンカリングと到達コストの切り分け）は
> 対象が無くなったので削除した。新しい設計で注意すべき点は以下。
>
> **① H2 の「欠損」は3種類を区別すること。** 同じ「人数なし」でも意味が違う。
> `count_unknown`（明示的な表明）× `taps_value`（値を触ったか）で層別する：
> **(a) 一度も触っていない (b) 触ったが消した (c)「わからない」と明言した**。
> ボタンなし条件では (c) が構造上ゼロになるので、**条件間比較は (a)+(b)+(c) の合計＝
> 「人数が入らなかった率」で行う**こと。(c) だけを比べると自明な結果しか出ない。
>
> **② H2 の裏側（偽の値）も測ること。** ボタンなし条件で増えるはずの「適当な数」は、
> 直接は観測できない。代理指標として **丸め傾向（5・10の倍数への集中）**、
> **クラス目安中央値との一致率**、**同一便の参加者間ばらつき（ICC）** を使う。
> ボタンなし条件でこれらが悪化していれば、「嘘のラベルが混ざった」証拠になる。
>
> **③ H3 は尺度の交絡に注意。** クラス幅が上位ほど広い（6→10→15→15→15→開放。2026-09-20改訂後）。
> 欠損率は幅に依存しないが、所要時間・タップ数を混雑度別に比べるときは
> **クラス幅で正規化**し、生の人数は参考として併記する。
>
> **④ 入力方式の標的サイズは揃っていない**（375×812実測）。
> ステッパーは値ボタン4個・57×56px、テンキーは12個・93×48px で**面積比1.40倍**。
> 形が違う以上そろえられないので、`screen_view` にボタン実寸（`ctl_w`/`ctl_h`）と
> 画面サイズを記録して解析で統制する。
>
> **⑤「わからない」ボタンありのほうが画面が44px高い。**
> ただし送信・スキップ行は下端固定（`position:sticky`）なので、**送信までの到達距離は変わらない**。

> ★2026-09-19 追記：**「わからない」を表明する手段が条件間で非対称だった問題を解消した。**
>
> 先行実験（2026-09-18）で判明：初期値あり条件では人数欄に数値が入っているが、
> 「—」に戻す手段がテンキー（`C`あり）とステッパー（なし）で**非対称**だった。
> そのため人数を数えられないときにステッパーでは0まで下げて送るしかなく、
> **「わからない」が「0人」として記録された**（3件）。これは①データが壊れる
> ②未入力率や所要時間の2方式比較が、操作性ではなく**機能の有無の差**になる＝交絡する、の二重の問題。
>
> **両入力方式に共通の「わからない」ボタン**を、入力方式の外側に追加して解消した（`uiStudy-0.7.7`）。
> ステッパーに `C` を足す案は、ボタン数とレイアウトが変わって上の標的サイズ交絡をさらに動かすため採らない。
>
> これは単なるクリアではなく**「数えられない」の明示的な表明**として扱う。
> `count_unknown` イベントと `context.count_unknown` により、解析では次の3つを区別できる：
> **(a) 一度も触っていない未入力 (b) 触ったが消した (c) 数えられないと明言した**。
> **「わからない率」自体が新しい従属変数**になり、H3（混雑時ほど歪む）を
> 「歪み」だけでなく「そもそも答えられない」側からも支える。

> 補足：事後アンケートの「実際に数えましたか／表示された数字に影響されたと思いますか」が、
> 正解の代わりに**主観側からアンカリングを裏付ける**役割を果たす（§8）。

---

## 3. 実験デザイン

**被験者内計画（within-subject）／実バス／1路線に固定（阪急バス・吹田営業所管内に確定。系統は下見のうえ決定、`EXPERIMENT_PLAN.md` §5・`docs/HANDOFF_hankyu.md` §4.1）**

> 対外発表では阪急バスとの承諾条件により、社名・具体的路線名を明記せず
> 「一般的な路線バスでの乗車データ」として記載する（`docs/HANDOFF_hankyu.md` §1）。

- 路線を1つに固定する（実験環境を変えないことを優先。路線間比較は次のステップ）
- 1回の実験で**複数名が同じ便に乗る**（→ 同一便での回答の一致度が測れる）
- **★2026-09-18 変更：「実験コード」は廃止**。便の識別は `ui_responses.answered_at`（回答時刻）と
  GPS位置、実験者側の乗車記録（下見と同じ形でメモ）を突き合わせて事後的に特定する運用にした
  （`docs/HANDOFF_hankyu.md` 参照）。参加者がアプリ内で何かを入力する必要はない

### 独立変数（2要因 × 2水準 ＝ 4条件）★2026-09-19 改訂

| 記号 | 要因 | 水準 | 切り替え方 |
|---|---|---|---|
| **A** | 入力方式 | `stepper`（−5/−1 [値] +1/+5） ／ `numpad`（自前テンキー） | アプリ内ローテーション、またはURLで固定 |
| **B** | 「わからない」ボタン | あり（既定） ／ なし（`?unknown=off`） | **URLのみ**（ローテーションしない） |

> **旧「B：初期値（none / median）」は廃止した**（理由は §1）。人数欄は常に `—` から始まる。
>
> **B をローテーションさせない理由**：ボタンが回答のたびに出たり消えたりするのは参加者に
> はっきり気づかれる。条件そのものへの反応（要求特性）を生むので、**便ごとに配布URLで固定**する。
> 入力方式の切り替えは値の作り方が変わるだけで、ここまで露骨ではない。

### 要因C：混雑水準（★2026-09-25 準独立変数から格上げ）

| 記号 | 要因 | 水準 | 切り替え方 |
|---|---|---|---|
| **C** | 混雑水準 | 空いている便（15時台）／混んでいる便（17時台以降） | **便の時間帯**（＝実施日の集合時刻） |

路線・区間・停留所・所要時間・運賃・のりばはすべて同じで、**時間帯だけが違う**。
混雑以外の条件差が入らないので、路線を変える案より交絡が少ない（§1）。

- 割り付けは**日単位**。1日で両水準を取ると拘束が3時間を超えるため、日によって集合時刻を変える
- 同じ参加者に別日でもう一度来てもらい、要因Cを被験者内にする。組めない場合は被験者間でも可
- 便の中でも混雑は変動する（始発直後が最も混み、住宅地へ進むにつれ空く）。
  **水準の割り付けは便単位、区間ごとの実際の人数は共変量**として扱う

### 条件の割当（実装済み・2026-09-19 改訂）

```
UI_CONDS = [ stepper/none, numpad/none ]      ← 要因A。1回答ごとに1つ進む
SHOW_UNKNOWN = URLで決まる                     ← 要因B。そのURLの間ずっと同じ
```

**配布URLの作り方**（`https://matsumototaichi-beep.github.io/bus-ui-study/` に付ける）：

| 目的 | URL |
|---|---|
| 入力方式をローテーション＋ボタンあり | （パラメータなし） |
| 入力方式をローテーション＋ボタンなし | `?unknown=off` |
| ステッパー固定＋ボタンあり | `?input=stepper` |
| ステッパー固定＋ボタンなし | `?input=stepper&unknown=off` |
| テンキー固定＋ボタンあり | `?input=numpad` |
| テンキー固定＋ボタンなし | `?input=numpad&unknown=off` |

- **入力方式を配布URLで固定する場合**：この要因については①（同じ人が両方を経験する＝被験者内比較）も
  ②（同じ瞬間に参加者間で条件を反転させる）も失われ、**便（日）単位の比較**になる。
  `EXPERIMENT_PLAN.md` §3.1・§8「答える人が違えば差も出るのでは」への回答が使えなくなる。
  **同一路線・同一時間帯で条件を入れ替えた便を対にして組む配置が必須**
- **要因B（ボタンの有無）は必ず便単位の比較になる**ので、上記の対配置はBについては必須
- URLで固定しなければ、入力方式は1回答＝1セッションごとに自動でローテーションし、
  `condOffset()` が端末ごとのランダムな開始位置を保存する
- 被験者は条件が切り替わっていることを知らされない
- DB側では `variant.input` / `variant.unknown_ui` / `variant.url_fixed`（`{input}`）で判別できる

### なぜ OS のキーボードを使わないか
テンキーをOS標準のソフトキーボードで実装すると、**表示遅延と画面占有**が交絡し、
「テンキーが遅かったのは入力方式のせいではなくキーボード待ちのせい」と反論されて終わる。
そこで**自前のテンキーを画面内に固定表示**する。副次的な利点：
ボタンサイズを統制できる／タップ座標が記録できる。

### 送信ボタンの固定
テンキーは縦に長く、そのままでは送信ボタンが画面外になる。
入力方式によって**送信への到達コストが変わる**のを避けるため、
送信／スキップ行をスクロール領域の下端に固定した（両条件に等しく適用）。

---

## 4. 被験者と規模

| | 値 | 備考 |
|---|---|---|
| 被験者数 | **12名**（最低10名） | 2026-09-24 決定。指導教員より「10名は必須」／4条件を均等に回すには4の倍数が扱いやすい |
| 1人あたり回答数 | **15〜18回答**（各条件4回以上） | 1乗車で複数回答（バス停ごと）して稼ぐ |
| 想定 | 1人2〜3乗車 × 各5〜6回答（2026-09-24 決定） | 1便あたり参加者3名 |
| 総セッション数 | 12名で **180〜216** | 1条件あたり45〜54 |

- 被験者内計画は個人差を除去できるため、被験者間計画より必要人数が大幅に少ない
- 正確な検出力分析は効果量の仮定が必要。**事前に数字を置かず、データが揃ってから事後に確認**する

---

## 5. 従属変数

### 時間
| 記号 | 定義 | 状況 |
|---|---|---|
| `t_total` | セッション全体 | ✅ |
| `t_class` | 報告画面表示 → クラスタップ | ✅ |
| `t_count` | 人数画面表示 → 送信/スキップ | ✅ |
| `t_first` | 人数画面表示 → 最初の値入力操作（**思考時間と操作時間の分離**） | ✅ |
| `t_stop` | バス停欄の滞在時間 | ✅（2026-09-20以降は「次に停まるバス停」1欄のみ） |

### 操作コスト
| 項目 | 意味 | 状況 |
|---|---|---|
| `taps_total` / `taps_value` | 総タップ数／値を作るための操作数 | ✅ |
| ±1/±5 の使用比率 | どの粒度が実際に使われたか | 🔧 |
| **オーバーシュート回数** | ＋の後に−へ方向転換した回数＝**押しにくさの客観指標** | 🔧 |
| テンキーの無効タップ数 | 3桁目を押した回数（`ignored:true`） | ✅ |
| `corrections` | 戻る／クリア／選び直し | ✅ |
| バス停：入力文字数・選んだ候補の順位・候補を出したが選ばなかった率 | 予測変換の当たり具合 | ✅ |
| **申告した「次のバス停」とGPS位置の距離** | **位置情報がどれだけ当てになるか**（§9 副5） | 🔧 `next_stop` と `lat`/`lng` から計算 |

### 回答内容（★中心）
| 項目 | 何が言えるか | 状況 |
|---|---|---|
| **丸め傾向**（5・10の倍数への集中） | **H2の裏側＝「適当な数」の代理指標**。ボタンなし条件で悪化していれば、嘘のラベルが混ざった証拠 | 🔧 |
| **クラス目安中央値との一致率** | 同上。数えずに「真ん中あたり」と答えた割合 | 🔧 |
| **クラス選択と入力人数の乖離** | 目安範囲外に入力した割合と方向 | 🔧 |
| **クラス別の乖離率** | どのクラス境界が曖昧か＝**クラス設計の妥当性** | 🔧 |
| 丸め傾向（5・10の倍数への集中） | 実数え上げか概算か | 🔧 |
| **同一便の被験者間ばらつき** | 信頼性（ICC）。正解なしで測れる | 🔧（`exp_code`は廃止。回答時刻・GPS・URLの組み合わせで便を事後的に特定して算出する） |
| 回答値の分布形状 | UIによる歪み | 🔧 |

### 完遂・欠損
送信完了率／離脱率（`abandon`）／スキップ率／バス停入力率／位置情報許可率 — すべて ✅

| 項目 | 何が言えるか | 状況 |
|---|---|---|
| **「わからない」率** | **数えられなさそのもの。H3を「答えられない」側から支える** | ✅ `context.count_unknown`（2026-09-19〜） |
| 欠損の内訳：(a)一度も触っていない (b)触ったが消した (c)わからないと明言 | 同じ「人数なし」でも意味が違う。条件間比較にはこの層別が必須 | ✅ `count_unknown` × `taps_value` × `count_touched` |
| 「わからない」に至るまでの操作量 | 諦めるまでにどれだけ操作したか＝**入力方式の負荷** | ✅ `count_unknown` イベントの `dt` と直前までの `taps_value` |

### 文脈・共変量
| 項目 | 状況 |
|---|---|
| 混雑度（クラス） | ✅ |
| **着席／立席** ← 片手操作かを左右する最大の交絡要因 | ✅ `posture`（ログアウトのたびにリセット） |
| 時間帯・曜日 | ✅ |
| 便の識別 | ➕ `exp_code` は2026-09-18に廃止。`answered_at`・GPS・実験者の乗車記録の突合で事後的に特定する |
| 端末サイズ | ✅ |
| 何回目のセッションか（学習効果） | ✅ `variant.seq` |
| 条件の開始位置（URLで固定されない残りの要因のみ） | ✅ `variant.offset` |
| **要因B：「わからない」ボタンを出したか** | ✅ `variant.unknown_ui`（2026-09-19〜） |
| **入力方式がURLで固定されていたか** | ✅ `variant.url_fixed`（`{input}`。null でなければURL固定） |
| 初期値の条件（廃止した旧要因） | ✅ `variant.count_init`。2026-09-19以降は常に `"none"`。**それ以前のデータには `"median"` が混在するので分析時に必ず絞る** |
| 参加者の識別 | ✅ ログインアカウント（`surveyor_id`）。**参加者番号は2026-09-18に廃止**（識別はログインID、条件はURLで決まるため不要になった） |
| **練習回答かどうか**（分析から外す） | ✅ `context.practice` ＋ `ui_responses.practice`。練習は本番の条件カウンタを進めない |

凡例：✅実装済み ／ 🔧既存ログから計算可能 ／ ➕要実装

---

## 6. ログ仕様（実装済み）

1回答＝1セッション。時刻は `performance.now()` 起点の相対 ms（`dt`）で記録し、端末時計のズレを回避する。

```json
{
  "session_id": "uuid",
  "started_at": "2026-09-10T10:12:33.120Z",
  "outcome": "submit | skip | abandon",
  "variant": { "input": "numpad", "count_init": "none", "unknown_ui": true, "seq": 7, "offset": 1,
               "url_fixed": { "input": "numpad" }, "practice": false },
  "context": {
    "congestion_class": 4, "exact_count": 31, "count_touched": true, "count_unknown": false,
    "next_stop": { "name": "五月が丘", "lat": 34.787456, "lng": 135.526811 }, "gps_points": 12,
    "screen_w": 375, "screen_h": 667, "lang": "ja", "app_version": "uiStudy-0.7.9",
    "posture": "stand", "practice": false, "response_cid": "uuid"
  },
  "summary": { "t_total": 18420, "t_class": 3110, "t_count": 15300, "t_first": 2100, "t_stop": 5200,
               "taps_total": 9, "taps_value": 5, "corrections": 1 },
  "events": [
    { "dt": 0,    "type": "screen_view", "screen": "count", "input": "numpad", "count_init": "none",
      "unknown_ui": true, "lo": 31, "hi": 45, "ctl_w": 93, "ctl_h": 48, "vw": 375, "vh": 812 },
    { "dt": 1240, "type": "numkey", "key": "3", "value": 3 },
    { "dt": 1880, "type": "numkey", "key": "8", "value": 38 },
    { "dt": 3010, "type": "tap", "target": "send" },
    { "dt": 3010, "type": "session_end", "outcome": "submit" }
  ]
}
```

イベント種別：`session_start` `screen_view` `tap` `stepper` `numkey` **`count_unknown`**
`stop_focus` `stop_input` `stop_candidates` `stop_select` `stop_clear` `session_end`

`count_unknown`（2026-09-19〜）は「わからない」ボタンのタップ。`value_before` に、
押す直前に入っていた値（初期値のまま押したのか、自分で入れた値を捨てたのか）を残す。
`taps_value`（値を作るための操作数）には数えない。値を作る操作ではないため。

保存先：localStorage → Supabase `ui_sessions`（**テーブル未作成**。§10）

---

## 7. 分析計画

★2026-09-19 改訂（要因Bを「わからないボタンの有無」に置き換えたことによる）。

1. **前処理** — CSV/JSON エクスポート → pandas。異常セッション（極端に長い＝中断）を規準を決めて除外。
   **`variant.count_init == "none"` で絞る**（2026-09-18 以前の `"median"` データを混ぜない）
2. **主分析（H2）** — 人数が入らなかったか否かをロジスティック回帰（被験者・便をランダム効果）
   - 目的変数：`exact_count is null`（(a)触っていない＋(b)消した＋(c)わからないと明言 の合計）
   - 説明変数：**ボタンの有無 × 入力方式 × 混雑度**
   - **ボタンの有無の主効果が H2、混雑度との交互作用が H3**
   - (c) だけを条件間で比べないこと（ボタンなし条件では構造上ゼロになる。§2 注記①）
3. **H2の裏側（偽の値）** — ボタンなし条件で「適当な数」が増えたかを、
   丸め傾向（5・10の倍数率）・クラス目安中央値との一致率・同一便のICC で見る（§2 注記②）
4. **H1（時間・操作量）** — 混合効果モデルで `t_count`・`taps_value`・`t_first` を目的変数に。
   説明変数は入力方式 × 混雑度。ボタン実寸（`ctl_w`/`ctl_h`）を共変量に入れて標的サイズを統制（§2 注記④）
5. **信頼性** — 同一便の回答について級内相関 ICC を条件別に算出。
   便の特定は回答時刻・GPS・実験者の乗車記録の突合で行う（`exp_code` は廃止済み）
6. **クラス設計の妥当性** — クラス選択と入力人数の乖離をクラス別に集計。
   先行実験で「しきい値と直感が合わない」と指摘があるため、**クラス境界の再設計の根拠になる**
7. **主観との突合** — アンケート得点と客観指標の一致／不一致（§8）

---

## 8. 事後アンケート

詳細は **`QUESTIONNAIRE.md`**。狙いは3つ。

★2026-09-19 改訂。

1. **条件別の主観評価** — ステッパーとテンキーを再提示して評価させる。
   入力方式をURLで固定した参加者は片方しか経験していないので、**もう一方は画面を見せて評価させる**
   （経験に基づく評価ではない点を論文に明記すること）
2. **回答行動の自己報告** — 「実際に数えたか／目測か」。ログでは分からない部分を埋める
3. **★H2の主観側：「適当に答えた」の自己申告** — 「数えられないまま、それらしい数を入れたことがあるか」
   「人数を数えるのは難しかったか」。
   **ボタンなし条件でこの自己申告が多ければ、H2の裏側（偽の値が混ざる）の直接の裏づけになる。**
   先行実験で実験者本人が「数えられないのに適当な数でいいよとしてしまう」と報告しており、
   ログだけでは捉えられない行動なので**必須**
4. **「わからない」ボタンの受け止め方**（ボタンあり条件のみ）— 「押すことに抵抗があったか」
   「サボっていると思われそうだと感じたか」。欠損率の解釈に効く

---

## 9. 副次的に書ける章（追加実験なしで出せる）

| | 内容 |
|---|---|
| 副1 | クラス設計の妥当性（同一便での一致度、クラスと人数の乖離） |
| 副2 | 混雑度と回答負担（所要時間・スキップ率・ばらつき）＝**欲しい時ほど取れないジレンマ** |
| 副3 | バス停予測変換の実態（入力文字数・候補順位・大阪の同名/類似名問題） |
| **副5** | **位置情報の信頼度**（2026-09-20 追加）。申告された「次に停まるバス停」とGPS位置を突き合わせ、**スマホのGPSだけでバス停・便をどこまで特定できるか**を測る。乗る路線・便はこちらで把握しているので検証できる。**NAIST側（BLEセンサーの位置情報紐づけ）に直接還元できる成果** |
| 副4 | ステップ幅の使われ方（±1/±5の使用比率・オーバーシュート）。±1/±5/±10 との比較は**本研究では行わない** |

---

## 10. 実装状況と残タスク

### 完了
- 人数入力のクラス制限撤廃（クラス範囲は「目安」と初回タップの起点のみ）
- ステッパー `−5 −1 [値] +1 +5`、値は常時枠表示（未入力＝破線／入力後＝青実線）
- **自前テンキー**と入力方式の条件切替、条件のローテーション、端末別オフセット
- 送信/スキップの下端固定
- 操作ログ（イベント・所要時間・タップ数・訂正回数）、localStorage 保存、JSON エクスポート
- 小画面対応（iPhone SE2/SE3・SE1 で検証済み）
- **着席／立席トグル**（報告画面上部。切り替えるまで保持。日付が変わる／ログアウトすると未設定に戻る）
- **`t_first`・`t_count`** の記録
- **結合キー**：回答（`ui_responses.session_id`）と操作ログ（`ui_sessions.session_id`）を突合できるようにした
- **2026-09-18**：条件割当のコマ消費を「クラスタップ時」→「送信/スキップ確定時」に修正（`peekVariant`/`commitVariant`）。
  クラスをタップして「戻る」でキャンセルしても、ラテン方格の1コマを消費しないようにした
- **2026-09-18**：ログアウト時に着席/立席・練習モードをリセットするよう修正
- **2026-09-18（設計変更）**：**参加者番号・実験コードを廃止**。参加者の識別はログインアカウント
  （`surveyor_id`）のみで行い、条件（入力方式・初期値）は**URLのクエリパラメータ**（`?input=stepper`等）で
  実験者が固定できるようにした。便の識別は回答時刻・GPS・実験者の乗車記録の突合による事後特定に変更
  （`docs/HANDOFF_hankyu.md` 参照。ゲート画面 `renderPrepGate` は不要になったため削除した）
- **2026-09-18：先行実験（下見）を実施**。阪急バス吹田市内線10系統（JR吹田駅南口16:19→桃山台駅）と
  2系統（桃山台駅17:18→JR吹田駅北口）に乗車し、アプリで39件回答しつつ全停留所の乗降人数を手記録。
  乗降記録から復元した車内人数は、便1で終点ちょうど0に閉じ、アプリの目視人数とも誤差ほぼゼロで一致した
  （整形済みデータは `exports/` 配下。GPS軌跡を含むため git 管理外。詳細は `docs/log/2026-09-19_hankyu.md`）
- **2026-09-19：両入力方式に共通の「わからない」ボタンを追加**（`uiStudy-0.7.7`）。
  ステッパーからは「—」に戻せず「わからない」が「0人」として記録される交絡を解消した
- **2026-09-19（設計変更）**：**初期値あり条件（アンカリング）を廃止し、要因Bを
  「わからない」ボタンの有無に差し替えた**（`uiStudy-0.7.8`）。`?unknown=off` で消せる。理由は §1。
  人数欄は常に「—」から始まる。配布URLは6通り（§3の表）
- **2026-09-20**：**クラスのしきい値を大型車基準に改訂**（`uiStudy-0.7.9`）。
  `16-25`→`16-30`（「席が埋まる」を大型車の座席数に合わせた）、以降 `31-45` / `46-60` / `61-`。
  吹田市内線で通常使われるのは大型車（座席24〜34席、法定定員76〜89人）で、
  旧しきい値は中型車（座席20〜28席）に近い値だった
- **2026-09-20（設計変更）**：**乗車バス停・降車予定バス停の入力を廃止し、「次に停まるバス停」
  1項目に置き換えた**（`uiStudy-0.7.9`）。旧2項目はNAIST側（BLEセンサーの位置情報紐づけ）のための
  項目で、BLEセンサーの無い阪急バスでは意味を持たなかった。新項目は**位置情報の信頼度**を測るためのもの（§9 副5）。
  DB側は `ui_responses.next_stop`（jsonb）を追加。**旧 `board_stop`/`dest_stop` 列は
  先行実験データが入っているので残してある**
- **2026-09-19**：バス停辞書を736件に更新。`七尾西` → `関西大学吹田みらいキャンパス` に改称
  （2025年7月改称。阪急バス公式のポールID 0758 が同一、関西大学公式にも記載）、`佐井寺南が丘` を追加
  （国土数値情報 令和4年度版には無い。2023年以降の新設）。**どちらも先行実験のルート上で、
  本番実験で参加者が選べない状態だった**

### 残タスク（優先順）
0. ~~**ステップ幅を ±1/±5 の2段階に戻すか決める（9月中）**~~ → **決定・実施済み**（`uiStudy-0.7.2`）。以下は判断の記録 — 先輩への約束「±1,5のみとどっちが良いかを比較する」が
   この一覧に無く、計画から落ちていた（`docs/senpai_feedback.md` §2②）。
   アプリ改修の凍結が10月上旬なので、戻すなら9月中。
   戻すとタップ標的が 41.7→58.1px(375px端末) に回復し、H2 の標的サイズ交絡（テンキーとの面積比 約2倍）も 1.4倍まで縮む。
   比較そのものは本実験に第3要因として足せない（8条件で検出力不足）ため、**本研究では行わない**（2026-09-21 決定）
1. ~~Supabase で `ui_responses.sql` と `ui_sessions.sql` を実行~~ → **2026-09-18 実施済み**。テスト送信で保存を確認
2. ~~実験版のホスティング~~ → **2026-09-18 実施済み**。GitHub Pages で公開
   （https://matsumototaichi-beep.github.io/bus-ui-study/ ）
3. **バス停辞書**（2026-09-17 実施済み。系統確定を待たず先行して差し替えた）
   - 国土数値情報 P11 大阪府(27)・令和4年度版から、事業者名に「阪急バス」を含む735件を抽出し `stops.js`
     （`window.HANKYU_STOPS`）を差し替えた。系統が確定したら候補系統の停留所名を阪急バス公式サイトで照合すること
     （令和4年度時点のデータなので、現行と食い違う可能性は残る）
4. 事後アンケートの実装（Google Forms。`QUESTIONNAIRE.md`）
5. 分析スクリプト（JSON → pandas）

---

## 11. 未決定事項

**2026-09-17 更新**：実施地域は阪急バス（吹田営業所管内）に確定、倫理審査は不要と確認済み（`docs/HANDOFF_hankyu.md`）。
残る未決定事項：

- ~~**対象系統**~~ → **2026-09-18 確定**：吹田市内線 **10系統**（JR吹田駅南口→桃山台駅）と **2系統**（桃山台駅→JR吹田駅北口）。
  残るのは**実施時期・実施日数・1人あたりの乗車回数**
- ~~管轄営業所への事前連絡~~ → **2026-09-24 連絡済み**（承諾条件④）。日程は松本が参加者の都合に合わせて決める
- ~~被験者の募集方法と謝礼~~ → **2026-09-24 決定**：**友人づて**で募集。謝礼なし（交通費は大学から出る見込み）
- ~~被験者数~~ → **2026-09-24 決定**：**12名**、1人2〜3乗車、1便あたり参加者3名
- ~~練習回答を入れるか~~ → **2026-09-24 決定**：**2回**入れる。**どこで行うかは未決**（`EXPERIMENT_PLAN.md` §3.2）
- ~~R3（ICカードの乗降記録）を使うか~~ → **2026-09-24 決定**：**使わない**
- ~~P0〜P3 を本実験の条件にするか~~ → **2026-09-24 決定**：**しない**
- ~~**事後アンケートの改訂**~~ → **2026-09-24 実施済み**。`QUESTIONNAIRE.md` を **v2.0** へ全面改訂（要因Aは S2 で直接比較、要因Bは S4 で最後に聞く。提示順のカウンターバランス用に**4版**を作る）。残るは Google Forms での作成と画面4枚の撮影
- **計数役を全便に入れるか、一部の日だけにするか**（`EXPERIMENT_PLAN.md` §2 R2）
- ~~**配布URLを何本に分けるか**~~ → **2026-09-19 決定**。要因Bをボタンの有無にしたので、
  §3 の表のとおり最大6通りから選ぶ。**要因B（ボタンの有無）は必ず便単位の比較**になるので、
  同一路線・同一時間帯で条件を入れ替えた便を対にして組む配置が必須
- ~~**クラスのしきい値（`COUNT_RANGES`）の再設計**~~ → **2026-09-20 実施済み**（§10）。
  0-5 / 6-15 / **16-30** / **31-45** / **46-60** / **61-** に変更。大型車の座席数（約30席）基準。
  **残る限界**：車両の大きさが日によって変わる（大型 座席24〜34席／中型 20〜28席）ため、
  単一のレンジで両方を正確に賄うことはできない。**論文に限界として書くこと**。
  また上位クラス（46〜60 / 61〜）は先行実験（最大約39人）で検証できていない
- ~~**乗車バス停・降車予定バス停の入力をどうするか**~~ → **2026-09-20 決定・実施済み**（§10）。
  **廃止し、「次に停まるバス停」1項目に置き換えた。**

  松本の説明：あの2項目は**NAIST側の目的のための項目**だった。バスターミナルなど多数のバスが
  重なる地点では、スマホのGPSだけではBLEセンサーと便を紐づけられないので、
  乗車地と行先を申告してもらって便を特定する狙いだった。
  **阪急バスにはBLEセンサーを設置していないので、この目的では全く意味を持たない。**
  「位置情報が取れない環境で便を識別するカバーになりうるか」という問いは
  **NAIST側で続けるのが先決**であり、卒研アプリには不要と判断した。

  代わりに置いた「次に停まるバス停」は、**位置情報がどれだけ当てになるかを測る**ための項目。
  乗る路線・便はこちらで把握しているので、**申告とGPSを突き合わせれば検証できる**（§9 副5）
