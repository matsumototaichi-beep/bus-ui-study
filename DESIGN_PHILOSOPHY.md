# 回答アプリのUI設計思想と改善シーケンス（v0.1）

> 教授の指摘（2026-09）：「このアプリから、iPhone や iPod のような *36万円払ってでも欲しいと思わせる* こだわり＝設計思想が感じられない」。
> このファイルは、①今のUIが**実際には何を根拠に作られているか**を正直に書き出し、
> ②どう改善し、③どう比較して、④院進後に「なぜこの形なのか」を説明できるようにするための作業台帳。

---

## 0. 結論から：いまのアプリには「一貫した設計思想」は無い

正直に書いておく。現在のUIは、**最初に1つの設計思想を決めて、そこから部品を導いたものではない**。
実態は次の3つの積み重ねである。

1. **スマホUIの一般的な定石**（Claude がデフォルトで使ったもの。§1-A）
2. **ユーザー（松本）からの逐次の要望**（ボタンを縦並びに、戻るボタンを左上に、送信ログを表示、など）
3. **制約への対処**（iPhone SE に収める、送信ボタンを画面外に出さない、など）

どれも個別には理由があるが、**「このアプリは誰の、どんな瞬間のための道具か」を1文で言える状態になっていない**。
教授が「哲学を感じない」と言ったのは、この点を正確に突いている。

---

## 1. 現在のUIの設計根拠の棚卸し

### A. 意図して入れた原則（根拠を説明できるもの）

| # | 原則 | 具体的な実装 | 根拠 |
|---|---|---|---|
| A1 | **最小の回答は1タップで成立させる** | クラス（6段階）を押した時点で回答として成立。正確な人数・バス停は任意 | 回答負担の最小化。数えられない人・急いでいる人も「クラスだけ」で参加できる |
| A2 | **任意項目は強制しない** | 人数・バス停・位置情報はすべて任意。未入力でも送信可 | ユーザー方針「任意で答えてくれる人を大切にする」（2026-09 決定） |
| A3 | **「数えていない」と「0人」を区別する** | 人数欄の初期値は `—`（破線枠）。0 とは別扱い | 教師データとして「未回答」と「0人」が混ざると壊れる |
| A4 | **操作の結果を必ず見せる** | 送信ログ（送信済／未送信／記録中）、トースト通知、ステータスバー | 送ったつもりで送れていない、を防ぐ（Norman の「フィードバック」） |
| A5 | **誤操作から戻れる** | 人数画面の「戻る」、バス停の「取消」 | 走行中の誤タップは避けられない前提 |
| A6 | **主要操作は親指の届く下端に置く** | 送信／スキップをスクロール領域の下端に固定 | 片手操作・立ち乗りを想定 |
| A7 | **押せる部品は十分に大きく** | ボタン高さ 44〜56px | Apple HIG の最小タッチ領域（44pt）に準拠 |
| A8 | **外部データは出典を画面に出す** | バス停候補の下に「国土数値情報（国土交通省）」 | ユーザー方針（出典の明示） |

### B. 要望や成り行きで決まり、原理的な根拠が弱いもの

| # | 現状 | 問題点 | 状態 |
|---|---|---|---|
| B1 | **ダークテーマ** | 最初のひな形のまま。夜間の眩しさ軽減という理屈は後付けできるが、**昼間の車内（窓からの外光）での見やすさは未検証** | 根拠なし |
| B2 | **6クラスを緑→赤の色で表現** | 信号機の比喩。**赤と緑は色覚の多様性で見分けにくい組み合わせ**。番号とラベルを併記しているので読めなくはないが、色が主な手がかりになっている | 要再検討 |
| B3 | **「少ない＝上、多い＝下」の縦並び** | 要望で決定。逆（多いが上＝満杯の比喩）や横並びとの比較はしていない | 比較未実施 |
| B4 | **戻るボタンが左上** | 要望で決定。しかし**片手・右手親指から最も遠い位置**で、A6 と矛盾する | 矛盾あり |
| B5 | **ヘッダーの情報量**（ブランド名・タブ・調査員名・言語・ログアウト） | 片手で3秒で答えるべき画面としては情報過多 | 要整理 |
| B6 | **クラスカードの幅（300px）・色の濃さ・余白** | 見た目のバランスで調整しただけ | 根拠なし |
| B7 | **クラスの区切り（0-5／6-15／16-25／26-40／41-55／56-）** | **バスの座席数と最大乗車数からの概算**（松本）。以前この欄に「元研究（BLE）の都合で決めた区切り」と書いていたのは誤り。元研究の区分は `0-5/6-10/11-20/21-30/31人以上` の5段階で、これとは別物（2026-09-17 確認） | **計算の内訳が未記録。**卒論・発表で必ず聞かれるうえ、`median` 条件のプリセット値がこの数値に依存するので、使った座席数・定員の数字を書き起こす必要がある。区切りの妥当性自体は実験の副次分析で検証予定（`UI_STUDY_SPEC.md` §9 副1） |

### C. 決定的に欠けているもの

**設計思想の1文（デザインステートメント）**。
すべての部品は「その1文に貢献するか」で残す・直す・捨てるを決めるべきだが、その基準が無い。

---

## 2. 設計思想の候補（ここを決めるのが最初の仕事）

叩き台として3案。どれを選ぶかで、画面は大きく変わる。

| 案 | 設計思想（1文） | ここから導かれること |
|---|---|---|
| **甲** | **「揺れる車内で、片手で、3秒で答えられる」** | 操作はすべて親指ゾーン。文字入力なし。ヘッダー最小化。戻るも下へ |
| **乙** | **「数えていない人を、数えたふりをさせない」** | 初期値は出さない（アンカリング回避）。クラスだけで堂々と送れる導線。正確な人数は「数えた人だけ」の別扱い |
| **丙** | **「答えた人が、自分の回答が役に立ったと分かる」** | 送信後に集計や他の人の回答を見せる。継続回答の動機づけ |

> 甲と乙は研究の主題（混雑時の回答品質・アンカリング）と直結する。
> 丙は参加率の研究（Tiramisu などの先行研究が扱った領域）に近い。
> **卒研としては甲×乙の組み合わせが自然**だが、決めるのは松本と教授。

---

## 3. 改善シーケンス（作業の進め方）

教授の方針：「今はこういう考えで作られている → もっと工夫するには → 逆に不便なものは → 誰でも思いつくやり方との差は → 何パターンか作って比較」。
これを手順に落とすと次のようになる。

```
Step 0  棚卸し        いまの設計根拠を書き出す（このファイルの §1）
Step 1  思想を決める   設計思想の1文を決める（§2）
Step 2  監査          全部品を「その1文に貢献するか」で 残す／直す／捨てる に分類
Step 3  パターン化     比較用のUIを複数作る（§4）
Step 4  比較          同じ課題・同じ指標で比べる
Step 5  記録          「原則 → 変更 → 結果」を §5 の台帳に残す
        └→ 本を読んで得た原則を Claude に指示し、Step 2 に戻る（繰り返し）
```

### 松本と Claude の役割分担
- **松本**：UIの本を読み、配置・色使い・情報設計の原則を拾ってくる。どの原則を採用するか決める
- **Claude**：指示された原則を実装し、**その原則のどこをどう反映したか**を必ず説明する。反映できない・矛盾する場合はそう言う
- **Claude が勝手に「良さそうな見た目」に変えない**。変更は必ず原則に紐づける（そうしないと §0 の状態に戻る）

---

## 4. 比較のためのUIパターン

「自分たちの案が良い」と言うには、**比較対象**が要る。HCI研究では、
**素朴な基準案**と、**わざと悪くした案**を並べて、測定方法そのものがちゃんと差を検出できるか確かめる、というやり方がある。

| パターン | 中身 | 何のために作るか |
|---|---|---|
| **P0 現行版** | 今のアプリ | 出発点 |
| **P1 素朴版** | 「人数を入力してください」＋数字入力欄＋送信ボタンだけ。**誰でも最初に思いつく形** | 「普通に作るとこうなる」との差を示す基準 |
| **P2 わざと不便な版** | 例：0〜99 のプルダウン選択、小さいボタン、送信前に確認ダイアログ | 測定指標（時間・タップ数・誤操作）が「悪さ」をちゃんと検出できるかの確認 |
| **P3 思想を突き詰めた版** | §2 で決めた思想を徹底した版（例：甲なら全操作を画面下半分に集約） | 提案手法 |

> **注意（実験計画との関係）**：本実験（実バス）の条件は現在「入力方式2×初期値2＝4条件」で確定している。
> P0〜P3 を本実験の条件にするなら条件数が増え、1条件あたりのデータが減る（`UI_STUDY_SPEC.md` §4）。
> そこで、**P0〜P3 の比較はデザイン探索として研究室内の短時間テスト（静止状態）で行い、
> 良かった案を本実験に持ち込む**、という二段構えを推す。本実験の条件自体を差し替えるかは教授と相談。

---

## 5. 変更台帳（院進後に「なぜこの形か」を説明するための記録）

**変更のたびに1行追記する。** 根拠のない変更はここに書けないので、書けないなら変更しない。

| 日付 | 変更 | 根拠（原則・文献・データ） | 結果・観察 | 版 |
|---|---|---|---|---|
| 2026-09-08 | クラス別の人数上限を撤廃。クラス範囲は「目安」表示に | 人数を強制的に範囲へ丸めると回答値そのものを分析できない | アンカリングの測定が可能になった | phaseA-1.4 |
| 2026-09-08 | ステッパーを ±1/±5/±10 に | 大人数時の操作回数削減 | **根拠が成立していない（2026-09-17 検算）。**実測データ（`rich_labeled_bus_data.csv` n=6597・中央値6人）で ±10 が1タップでも減らすのは 2.8%、平均削減 0.03タップ。一方ボタン幅は 58px→35.3px(320px端末)/41.7px(375px端末) と全機種で HIG 44pt 未満になり、原則 A7 に反する。先輩の「両方あるとUIが悪くなるかも」が寸法・データの両方で裏付けられた形。**2026-09-17 に ±1/±5 へ差し戻した**（下行） | phaseA-1.4 |
| 2026-09-10 | 人数表示をボタン行の中央に移し、未入力は破線枠 | 初見で「±が何を増減させるか」が分からないという指摘 | 未評価 | phaseA-1.5 |
| 2026-09-10 | 自前テンキーを追加（OSキーボード不使用） | OSキーボードの表示遅延・画面占有が比較の交絡になる | — | uiStudy-0.4 |
| 2026-09-10 | 送信／スキップを下端に固定 | テンキー条件だけ送信ボタンが画面外になり到達コストが変わる（A6） | 4条件すべてで常時表示を確認 | uiStudy-0.4 |
| 2026-09-13 | 実験バーに**参加者番号**を追加。条件の開始位置を `(番号-1) mod 4` にし、番号を変えたら試行カウンタを 0 に戻す | `EXPERIMENT_PLAN.md` §3.1 のラテン方格。端末ごとのランダム開始だと同じ便の参加者で条件が偶然かぶり、「同じ瞬間・同じ車内を違う条件で見る」が崩れて便の違いを相殺できない。カウンタのリセットは §3.2-3 の統制変数「何回目の回答か」に前の参加者の回数が混ざるのを防ぐため | 参加者1〜4で条件が1つも重ならないことを確認。実験バーが3要素になったため折り返しを許可（320×568 でもはみ出し 0） | uiStudy-0.6 |
| 2026-09-13 | **練習モード**を追加（実験バーのトグル＋オン中は画面上部に橙のバナー、タップで終了）。練習中は専用カウンタを使い、本番の条件の順番を進めない | 計画書 §3.2-1 の練習試行を本データから確実に外すため。練習が本番カウンタを進めると、練習回数が人によって違うぶん本番の開始条件がずれ、ラテン方格が崩れる。バナーを出すのは、小さなトグルだけだと切り忘れたときに本番データが全部「練習」になるため | 練習2回→本番1回目が `stepper/none seq=0`（練習なしの場合と同一）になることを確認。320×568 ではみ出し 0 | uiStudy-0.7 |
| 2026-09-17 | ヘッダーのナビ文字を折り返させない（`white-space:nowrap`）。ブレークポイントを 360px→400px に広げ、320px級ではログイン名を非表示 | 管理者表示（データ一覧ボタンあり）でボタン文字が縦に折返し、ヘッダーが 375px で 120.8px まで肥大して送信ボタンを画面外へ押し出していた。**先輩は共有管理者アカウントで開くため、先輩が見る画面がこれだった** | 320×460／320×568／375×559／375×667／390×664／667×375 の管理者表示・長い表示名で、ヘッダー43.6px固定・ナビ切れなし・横はみ出し0 | phaseA-1.6 / uiStudy-0.7.1 |
| 2026-09-17 | 送信／スキップをスクロール領域の下端に固定（本体側へ移植） | uiStudy-0.4 で卒研側に入れた対応が本体に入っていなかった。画面の低い端末・横向き・キーボード表示中に送信ボタンが画面外に出る | 横向き(667×375, 要スクロール164px)・SE1実効高(320×460, 79px)でも送信ボタンに常に届くことを確認 | phaseA-1.6 |
| 2026-09-17 | 人数入力中は描き直しでクラス一覧に戻さない。入力途中の値を `pending` に保持 | ナビ「報告」や言語切替で `renderMain()` が走り、入力値が消えたうえ `pending` が残って、次のクラス選択で `exact_count=null` の行が画面ログに出ないまま送信されていた | 入力26→言語切替→ナビ報告 で値が保持。卒研側では加えて `count_view_at` を取り直さないようにし（`t_count`・`t_first` のリセット防止）、描き直しは `screen_redraw` として記録。条件カウンタが進まないことも実測 | phaseA-1.6 / uiStudy-0.7.1 |
| 2026-09-17 | 版番号をステータスバーに常時表示 | どの版を見ているか画面から分からず、修正済みかの確認で食い違う（キャッシュで旧版のままの場合もある） | ブランド名に付けると320pxでナビを押し出したため下部に置いた | phaseA-1.6 / uiStudy-0.7.1 |
| 2026-09-17 | **ステッパーを ±1/±5 の2段階に戻す**（−5 −1 [値] +1 +5） | ±10 の削減効果が実データで平均0.03タップ・寄与2.8%しかない一方、7列だとボタン幅が全機種で HIG 44pt 未満になり原則 A7 に反する。先輩の「両方あるとUIが悪くなるかも」が寸法・データの両方で裏付けられた（`docs/senpai_feedback.md` §2②） | ボタン幅 320px→49.1px / 375px→58.8px / 390px→61.5px で全機種 44pt 達成。H2 の標的面積比（テンキー÷ステッパー）は 375px 1.95→**1.40**倍、320px 2.53→**1.82**倍。**解消はしていない**ので、`screen_view` にボタン実寸(`ctl_w`/`ctl_h`)と画面サイズを記録して解析で統制できるようにした | phaseA-1.7 / uiStudy-0.7.2 |
| 2026-09-17 | **バス停辞書を奈良県+京都府版から阪急バス（大阪府）版へ差し替え**（`stops.js`、`window.NARA_STOPS`→`window.HANKYU_STOPS`、735件） | 実施事業者が奈良交通から阪急バスに変わった（`docs/HANDOFF_hankyu.md`）。系統は下見待ちで未確定だが、辞書の差し替え自体は系統によらず先行して行える | 国土数値情報 P11 大阪府(27)・令和4年度版から事業者名「阪急バス」を含む停留所を抽出。候補系統A〜Cの主要停留所（JR吹田駅・桃山台駅・千里中央・江坂駅前・阪急曽根駅など）が含まれることを確認 | uiStudy-0.7.3 |
| 2026-09-18 | **条件（variant）割当のコマ消費を「クラスタップ時」から「送信/スキップ確定時」に変更**。`nextVariant()` を `peekVariant()`(覗くだけ)/`commitVariant()`(実際に1つ進める)に分離 | 松本の指摘：クラスをタップして「戻る」でキャンセルしても、旧実装ではその場でカウンタが進んでしまい、ラテン方格の1コマが未回答のまま消費されていた。送信/スキップが確定した回答だけをコマ消費の対象にすべき | JSコンソールで peek/commit を単体確認：キャンセル相当（commitしない）を何度繰り返しても seq が進まないこと、commit後は+1、同じ variant を2回commitしても多重に進まないことを確認 | uiStudy-0.7.4 |
| 2026-09-18 | **参加者番号を新規登録フォームに統合**。実験コードは「回答画面に入る前に必ず一度表示される確認ゲート」を新設して入力させる（ログイン画面ではなく、ログイン後・報告画面より前） | 松本の要望：「アプリに入ってから（実験バーの小さいボタンで）書くのは面倒、実験中に説明するのも難しい」。ただし実験コードは便が変わるたびに変わる値なので、ログイン画面（通常1回しか出ない）には統合できない。参加者は計画上1日だけの参加なので、参加者番号は新規登録時に一度取れば足りる | 新規登録フォームに参加者番号欄が出ること、ゲート画面で入力した値が `uistudy_pid_v1`/`uistudy_expcode_v1` に保存され `renderMain()` に遷移することをブラウザで確認。既存の実験バーの小ボタン（便ごとの実験コード変更用）は変更していない | uiStudy-0.7.4 |
| 2026-09-18 | **ログアウト時に着席/立席・練習モードをリセット**（実験コード・参加者番号は残す） | 松本の指摘：ログアウトして再ログインしても着席/立席がそのまま残っていた。次にログインする人（同じ参加者の再開でも、別の参加者への引き継ぎでも）のために、その時々の状態は消すべき。実験コード・参加者番号は同じ参加者が再ログインするだけなら変える必要がなく、参加者が変わる場合は新規登録でPIDが入り直るので、あえて残した | ブラウザでpostureをセット→ログアウトボタンをクリック→`uistudy_posture_v1`/`uistudy_practice_v1` がともに null になることを確認 | uiStudy-0.7.5 |
| 2026-09-18 | **参加者番号・実験コードを完全廃止し、条件（入力方式・初期値）をURLクエリパラメータで固定できる仕組みに置き換え**（`?input=stepper&init=median`）。指定しない軸は今までどおりアプリ内でローテーション | 松本の提案：①参加者の識別はログインアカウント（`surveyor_id`）で足りるので参加者番号は不要 ②「入力方式ごとに別URLのアプリを配布し、同じ便の参加者全員に同じURLを使わせる」運用にするので、アプリ内で条件を決める必要がなくなる ③便の識別（実験コード）も、回答時刻・GPS・実験者の乗車記録の突合で事後的に行えるので不要。この変更で①（被験者内比較）②（同じ瞬間の参加者間反転）は入力方式について失われるが、これは事前に説明のうえ松本が承知した上での判断（`UI_STUDY_SPEC.md` §11） | ブラウザで `?input=stepper` 指定時に入力方式が常にstepper・初期値だけがローテーションすること、`?input=stepper&init=median` 指定時に完全固定（1条件のみ）になり `variant.url_fixed` に記録されることを確認。新規登録フォームからPID欄が消えたことも確認 | uiStudy-0.7.6 |
| 2026-09-19 | **両入力方式に共通の「わからない」ボタンを追加**（人数表示の直下、入力方式の外側） | 2026-09-18の先行実験で判明：初期値あり条件では人数欄に最初から数値が入っているが、「わからない」に戻す手段がテンキー（`C`あり）とステッパー（なし）で**非対称**だった。そのため人数を数えられないときにステッパーでは0まで下げて送るしかなく、**「わからない」が「0人」として記録された**（3件。id=37/41/49、値操作タップ10〜13回）。①データが壊れる ②未入力率や所要時間の2方式比較が、操作性ではなく**機能の有無の差**になる＝交絡する、の二重の問題。**ステッパーに`C`を足す案は採らなかった**：ボタン数とレイアウトが変わり、H2の標的サイズ交絡（面積比1.40〜1.82倍）をさらに動かすため。入力方式の**外側**に同一の要素として置けば交絡しない。単なるクリアではなく「わからない」の明示的な表明として扱い（押すと枠が青くなる／値を入力し直すと解除）、`count_unknown` イベントと `context.count_unknown` で記録する。これにより「一度も触っていない未入力」「触ったが消した」「数えられないと明言した」を解析で区別でき、**「わからない率」自体が従属変数になる** | ブラウザで4条件すべてを実測：stepper/median は48から始まり −1 で47、**わからない で「—」に戻ること**（＝非対称性の解消）、ボタンが `on` 表示になること、その後 +1 で42（クラス下限41起点）になり表明が解除されることを確認。numpad/median・stepper/none・numpad/none でも同様。375×812でレイアウト崩れとコンソールエラーが無いことも確認 | uiStudy-0.7.7 |
| 2026-09-19 | **バス停辞書を736件に更新**。`七尾西` → `関西大学吹田みらいキャンパス` に改称、`佐井寺南が丘` を追加 | 先行実験のルート上に、アプリの候補に出ない停留所が2つあった（＝本番で参加者が選べない）。辞書は国土数値情報 令和4年度版なので、その後の新設・改称が反映されていなかった。改称の根拠は、阪急バス公式の**ポールID 0758 が同一のまま名称だけ変わっている**こと（2025年1月のアーカイブは「七尾西」）と、関西大学公式の「バス停名称が『七尾西』から変更となりました」（2025年7月）。`佐井寺南が丘` の座標はOSMの実地調査済みポール2本の中点（34.779951, 135.517785）。**表記は「ヶ丘」ではなく「が丘」** | ブラウザで `window.HANKYU_STOPS` を確認。総数736、`関西大学吹田みらいキャンパス` が1件あり `七尾西` が消えていること、`佐井寺南が丘` が追加されていることを確認 | uiStudy-0.7.7 |
| | | | | |

---

## 6. 読む本の候補（UIの配置・色・情報設計）

原則を拾う元として。**読んだら §5 の根拠欄に「書名・章」を書けるようにする**。

| 書名 | 何に効くか |
|---|---|
| D. A. ノーマン『誰のためのデザイン？ 増補・改訂版』 | アフォーダンス・シグニファイア・フィードバック・対応づけ・制約。**「なぜ使いにくいか」を言葉にする基礎** |
| スティーブ・クルーグ『超明快 Webユーザビリティ』 | 「考えさせない」設計、簡易ユーザーテストのやり方 |
| ロビン・ウィリアムズ『ノンデザイナーズ・デザインブック』 | 近接・整列・反復・コントラストの4原則、配色の基礎 |
| Apple Human Interface Guidelines（Web・無料） | タッチ領域、レイアウト、色、ダークモードの公式な基準 |
| Material Design ガイドライン（Web・無料） | 同上（Android 側の基準） |

> ここに挙げた本は、この会話の中で Claude が本文を読んで確認したものではない（一般的に定番とされる入門書）。
> 実際に読んで使えたものだけを残し、合わなかったものは消すこと。
