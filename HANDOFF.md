# 引継ぎ書：卒業研究「移動中の人数回答UI」（bus-ui-study）

> 作成：2026-09-13。別アカウントの Claude Code へ引き継ぐためのもの。**同じPC・同じファイルを操作する前提**。
> このファイルは「研究の経緯・調べたこと・決めたこと・決まっていないこと」を1か所にまとめたもの。
> 各論の詳細は、ここから案内する文書にある。

---

## 0. 最初にやること

### 別アカウントに貼り付けるプロンプト
```
これから卒業研究「移動中の人数回答UI」の作業を引き継ぎます。
まず C:\Users\taichi\Desktop\研究\bus-ui-study\HANDOFF.md を最後まで読んでください。
次に同じフォルダで git log --oneline -10 と git status を実行し、前任の変更を確認してください。
メモリ（C:\Users\taichi\.claude\projects\C--Users-taichi-Desktop-----ikenaga-san\memory\）も読んでください。
読み終えたら、現状と「決まっていないこと」を短くまとめて私に報告してから作業を始めてください。
```

### 読む順番
1. **このファイル**（全体像・経緯・調査内容・未決事項）
2. `EXPERIMENT_PLAN.md`（最新の実施計画。奈良交通前提）
3. `UI_STUDY_SPEC.md`（仮説・条件・ログ仕様。※§6 に計画書との食い違いあり）
4. `LITERATURE.md`（文献。どこまで本文を読んだか）
5. `DESIGN_PHILOSOPHY.md` / `QUESTIONNAIRE.md` / `WORKFLOW.md` / `docs/log/`

---

## 1. このプロジェクトは何か

- ユーザー（松本さん）は学部生。**卒業研究**として取り組んでいる。進学先は NAIST（奈良先端科学技術大学院大学）の予定。
- もともと NAIST の共同研究で「**バス車内の人数を乗客にスマホで答えてもらい、BLE（Bluetooth）による人数推定の正解データにする**」アプリを作った（NAIST 提出用アプリ）。BLE 人数推定は**進学後の本研究**。
- 卒研は方向を変え、**そのアプリ自体を題材にしたUI（ユーザーインターフェース）研究**にした。
- **最終目的（1文）**：移動中の市民センシングにおいて、回答UIの設計が「集まるデータの量と質」をどう左右するかを実証し、状況（混雑）に応じた回答UIの設計指針を示す。
- **本研究との接続**：BLE推定は教師データの質がすべて。卒研で「どう聞けば歪まないデータが取れるか」を示せば、院の研究の前提になる。**ただし BLE の推定精度そのものには卒研は貢献しない**（ユーザーにもそう説明済み）。
- 論文は「バスアプリの話」ではなく「**移動中に数を報告させるUI一般の話**」として書く（HCI研究の成果物はアプリではなく一般化できる知識、とユーザーに説明済み）。

---

## 2. 場所と構成

| 場所 | 中身 | 注意 |
|---|---|---|
| `研究\bus-ui-study\` | **卒研の本体**（このリポジトリ）。ローカルgit、**リモート未設定** | リモートが無いのは、NAIST提出アプリのリモートへ誤ってpushしないための意図的な措置 |
| `研究\bus-ui-study-refs\papers\` | **読んだ論文のPDFと抽出テキスト**（§5） | **Git管理外。GitHubに上げない**（論文の再配布は著作権の問題） |
| `研究\★アプリケーション\naist-bus-congestion\` | NAIST提出用アプリ。GitHub Pages で公開中（`phaseA-1.5`） | **原則触らない**。ユーザーが「本来とコピーどちらも」と明示したときだけ両方に同じ修正を入れる。**pushは必ずユーザーの許可を得てから** |
| `研究\★アプリケーション\handoff_bus_app.md` | NAIST アプリ側の引継ぎ書 | Supabase・管理者・保留中のセキュリティSQLなどはこちら |
| `研究\★アプリケーション\traj_extract\` | BLE データとの突合エンジン | **卒研では使わない**（ユーザー方針：UI研究ではBLEと紐づけない） |
| `研究\★ikenaga_san\.claude\launch.json` | 動作確認用のローカルサーバー設定（§9） | |
| メモリ `C:\Users\taichi\.claude\projects\C--Users-taichi-Desktop-----ikenaga-san\memory\` | `ui-study-project.md`（卒研の要点）、`read-full-papers.md`（文献ルール）、`explain-data-provenance.md`（出典明示）、`bus-hitl-project.md`（NAIST側） | 同じPC・同じプロジェクトなら読めるはず |

### bus-ui-study の中身
| ファイル | 役割 | 状態 |
|---|---|---|
| `index.html` | 実験用アプリ本体（**現行 `uiStudy-0.7.9`**。下表の記述は2026-09-17時点） | §4 |
| `stops.js` | **現行：阪急バス736件**（国土数値情報 P11・大阪府・令和4年度＋2026-09-19の改称/追加）。※旧記述「1236件・奈良県＋京都府・2010年」は2026-09-17に差し替え済み | 差し替え済み |
| `ui_responses.sql` / `ui_sessions.sql` / `ui_report_feed.sql` | Supabase のテーブル定義 | **2026-09-18 に実行済み**（`next_stop` 列の追加も2026-09-20に実行済み） |
| `viewer.html` | 軌跡ビューア（元アプリから流用） | |
| `UI_STUDY_SPEC.md` | 仮説・条件・従属変数・ログ仕様・分析計画 | v1.0 |
| `EXPERIMENT_PLAN.md` | **実施計画の大枠**（奈良交通前提） | v0.1、2026-09-12 |
| `QUESTIONNAIRE.md` | 事後アンケート（Google Forms 用） | v1.0、未実装 |
| `DESIGN_PHILOSOPHY.md` | UIの設計根拠の棚卸し・改善シーケンス・変更台帳 | v0.1 |
| `LITERATURE.md` | 文献メモ（読んだ範囲・該当ページ） | |
| `WORKFLOW.md` | 教授との共有・文献調査のルール | |
| `docs/log/2026-09-12.md` | 教授との打ち合わせの論点と未決事項 | |

---

## 3. 研究の経緯（何を決め、何を捨てたか。理由つき）

**同じ議論を蒸し返さないために重要。** 特に「却下された案」は、ユーザーが明確に却下している。

| 日付 | 出来事・決定 | 理由・補足 |
|---|---|---|
| 9/5〜6 | BLE研究から UI 研究へ方向転換。NAIST アプリ（`ee61321`）をコピーして `bus-ui-study` を作成。操作ログを計装 | 元アプリは提出物なので残す |
| 9/6〜7 | 研究の最終目的を議論。「どのUIが速いか」だけでは主張が弱い → **回答の質（データの歪み）**を軸にする方向に | |
| 〃 | ✗ **Claude が「ラボ（模擬）実験が主」と前提したが、ユーザーが訂正**：**実際にバスに乗ってもらう**。大学で志願者を募る | Claude の思い込みだった。**実験の前提を勝手に置かないこと** |
| 〃 | ✗ **実験者が同乗して車内人数を数える案 → 却下**（実験者も間違える） | ⇒ 「正確さ」は測れない前提に。主張を**系統的なバイアス（条件間のズレ）と信頼性（人によるばらつき・ICC）**に置き換えた |
| 〃 | 便IDは事業者と連携していないので取れない → **1回の実験で複数人が同じバスに乗り、実験者が「実験コード」を配って紐づける**。**1路線に絞る**（環境を変えないことが大事。路線間比較は次のステップ） | ユーザーの提案 |
| 〃 | 参加者：**最低10人（教授から「10人いないと研究として成り立たない」）**、理想20〜30人。人脈次第 | |
| 9/8 | **両アプリ**で人数入力の**クラス制限を撤廃**（どのクラスでも0〜99+）、ステッパー ±1/±5/±10、iPhone SE 対応 | 先輩の助言（小画面が窮屈）。制限撤廃により、初期値が回答を引き寄せる効果（アンカリング）が**初めて測定可能**になった |
| 9/9 | **両アプリ**で人数表示を `−10 −5 −1 [人数] ＋1 ＋5 ＋10` の行内に移動、未入力 `—` は破線枠。NAIST 版は push 済み（`phaseA-1.5`） | ユーザーの緊急修正依頼（「—」だと±が何を増減するのか分からない） |
| 9/10 | **入力方式（ステッパー／テンキー）を条件に追加** → **入力方式2 × 初期値2 ＝ 4条件**で確定 | ユーザーが「±ボタンか数字入力か」も精査したいと希望 |
| 〃 | テンキーは**自前実装**（OSキーボードは使わない） | OSキーボードの表示遅延と画面占有が交絡し、「遅いのはキーボード待ちのせい」と反論されるため |
| 〃 | ✗ **補助実験A（正解既知の映像で数えさせる）・B（揺れの影響）→ 不要**。**事後アンケート（C）のみ** | 「映像を作るのが大変」。アンケート結果とアプリの回答データから研究を絞りたい |
| 9/12 午後（別セッション） | 実験コード入力・着席/立席トグル・`t_first`/`t_count`・回答⇔操作ログの結合キーを実装（`12ccd92`, `uiStudy-0.5`） | 実験コードと姿勢は**日付が変わると未設定に戻る**（前日のコードで別の便と誤結合するのを防ぐ） |
| 9/12 | **教授との打ち合わせ**（§6） | |
| 9/12 夕 | 類似研究を調査（本文を読んだ）→ `EXPERIMENT_PLAN.md` ほか5文書を作成（`07d80dd`） | §5 |
| 9/13 | **条件の開始位置を実験者が指定できるように**（実験バーに「参加者」番号）。計画書 §3.1 のラテン方格が成立（`03e1a7a`, `uiStudy-0.6`） | それまでは端末ごとのランダム開始だったため、同じ便の参加者で条件が偶然かぶり、便の違いを相殺できなかった |

---

## 4. アプリの現状（uiStudy-0.7）

> ⚠️ **この節は 2026-09-17 時点の記述で、すでに古い。** 現行は `uiStudy-0.7.9`。
> 以降に次の大きな変更が入っている（正は `docs/HANDOFF_hankyu.md` §3 と `UI_STUDY_SPEC.md`）：
> - **2026-09-18**：参加者番号・実験コードを廃止（識別はログインアカウント、条件は配布URLで固定）
> - **2026-09-19**：**初期値（アンカリング）要因を廃止**し、要因Bを**「わからない」ボタンの有無**に差し替え。
>   人数欄は常に「—」から始まる。条件は `[stepper/none, numpad/none]` × ボタン有無の2×2
> - **2026-09-20**：**クラスのしきい値を大型車基準に改訂**（`0-5 / 6-15 / 16-30 / 31-45 / 46-60 / 61-`）。
>   **乗車バス停・降車予定バス停の入力を廃止**し、「次に停まるバス停」1項目に置き換え
>
> 下の記述のうち「参加者番号」「実験コード」「初期値 median」「乗車/降車バス停」
> 「クラスの区切り 16-25/26-40/41-55/56-」に関する部分はもう成立しない。
> **この注記より上の §1〜§3 も2026-09-17時点の記述**なので、現状は `docs/HANDOFF_hankyu.md` を見ること。

- **2段階の回答**：クラス（6段階）を1タップ → 人数画面（任意）。人数・バス停・位置情報はすべて任意（**強制しない**のがユーザー方針）
- **4条件のローテーション**：`UI_CONDS = [stepper/none, numpad/median, stepper/median, numpad/none]`。1回答ごとに次の条件へ
  - 開始位置は**実験バーの「参加者」番号**で決まる（`(番号-1) mod 4`）。同じ便の参加者1〜4が**同じ瞬間に必ず別々の条件**になる＝計画書 §3.1 のラテン方格
  - 参加者番号を変えると**試行カウンタ `seq` が 0 に戻る**（前の参加者の回数が「何回目の回答か」に混ざらないように）
  - 未設定なら従来どおり端末ごとのランダム開始（下見・パイロット用）。どちらで決まったかは `variant.offset_src`（`participant` / `random`）に残る
- **練習モード**（`uiStudy-0.7`）：実験バーの「練習」トグル。オン中は画面上部に橙のバナーが出て、タップで終了できる
  - オン中の回答は `practice: true` で記録（操作ログ側 `context.practice` と回答側 `ui_responses.practice` の両方）。分析では `practice = false` で絞る
  - **練習は専用カウンタ（`uistudy_pseq_v1`）を使い、本番の条件の順番を進めない**。練習回数が人によって違っても本番の割当がずれない
  - 実験コード・参加者番号と同じく日付で失効。参加者番号を変えると練習カウンタも 0 に戻る
  - ⚠️ **何回練習するか（そもそも入れるか）は未定**
- **初期値**：`none`＝`—`（破線枠）／`median`＝選んだクラスの目安範囲の中央値をプリセット
- **テンキー**：プリセットは最初の数字キーで置換、以降は追記。2桁上限（3桁目は無視して枠を一瞬光らせる、`ignored:true` で記録）。`C`＝全消去、`⌫`＝1桁削除
- **報告画面上部の実験バー**：実験コード（英数字とハイフン・8文字まで）、着席／立席トグル。未設定は橙色
- **送信／スキップはスクロール領域の下端に固定**（テンキー条件だけ送信ボタンが画面外になり到達コストが変わるのを防ぐ）
- **ログ**：1回答＝1セッション。`dt`＝セッション開始からの相対 ms（端末時計のズレ対策）。イベント・所要時間・タップ数・訂正回数を記録。仕様は `UI_STUDY_SPEC.md` §6
- **保存先**：いまは**端末の localStorage のみ**。Supabase のテーブルが未作成なので送信は失敗する（＝NAIST 側のデータは汚れない安全側）
- **データ分離**：Supabase のプロジェクトは NAIST アプリと同じだが、テーブルは `ui_responses` / `ui_sessions` と別。**NAIST の `responses` テーブルには触らない**

---

## 5. 文献調査の内容 ★今回いちばん引き継ぎたいところ

### 5.1 調査のルール（教授の方針・ユーザーが明示）
- **アブストや検索結果の要約だけで研究の方向性を決めない。本文のPDFを取得して読む。**
- どこまで読んだか（全文／該当章／アブストのみ）と、数値が載っていたページを `LITERATURE.md` に記録する
- 検索要約や他の論文での引用で知った情報は「**二次情報**」と明記する
- **実際に起きた失敗**：Tiramisu の「満員度は4段階（many seats〜full）」という情報を、検索要約から拾って一度ユーザーに伝えてしまった。本文テキストでは確認できず（図の中にしかない）、訂正済み。**同じことをしないこと**

### 5.2 調べ方（再現できるように）
- 使った検索語（英語）：
  - `TCRP Synthesis 77 Passenger Counting Systems manual ride check accuracy pdf`
  - `Tiramisu transit crowdsourcing bus fullness Zimmerman CHI 2011`
  - `Liu Conrad "Where should I start" default values slider questions web surveys`
  - `Izard Dehaene 2008 calibrating the mental number line numerosity estimation`
  - `van Berkel "The Experience Sampling Method on Mobile Devices"`
  - `crowdsourced passenger reported crowding accuracy compared automatic passenger counter`
  - `Caine 2016 "Local Standards for Sample Size at CHI"`
  - `Trick Pylyshyn 1994 subitizing counting`
- 日本語：`奈良交通 CI-CA ICカード 乗車時 降車時 タッチ`、`奈良交通 学園前駅 奈良先端科学技術大学院大学 バス 運賃`
- **PDFの取り方**：`curl -sSL -A "Mozilla/5.0"` で取得し、先頭が `%PDF` か確認する。
  **ACM Digital Library・ResearchGate・出版社（SAGE 等）は失敗しやすい**（HTMLのアクセス拒否ページが返る）。
  **著者の個人サイト・大学のリポジトリ・core.ac.uk・公的機関（TRB 等）**を探すと取れることが多い。
  例：van Berkel は ACM では取れず、著者サイト `nielsvanberkel.com/files/publications/csur2017a.pdf` で取得できた
- **PDFの読み方**：この PC では Read ツールで PDF を描画できない（`pdftoppm` 未導入）。**Python の `pypdf`（導入済み）でテキストを抽出して読む**。抽出済みの `*.txt` が `bus-ui-study-refs/papers/` にある（ページ区切りは `=== PAGE n ===`）

### 5.3 本文を読んだ文献と要点
| 文献 | 読んだ範囲 | 要点（本文で確認） | 計画での使い道 |
|---|---|---|---|
| **Boyle (1998) TCRP Synthesis 29**（米国交通研究・乗客計数の手法の総覧） | 要旨・第1,3,4,6,8章 | 手作業の計数＝調査員が乗って**全停留所の乗降を記録**（印刷p.4）。他の技術の**比較基準**になる（p.11）。誤りの原因は記録ミス・転記ミス・調査員の疲労（p.11）。「**どの技術でも100%の精度はない**」（p.3）。自動計数機も通常時の精度は業者の言う**85%**程度、使えるデータは平均75%（第6章）。検証として「**乗車総数と降車総数の不一致**」「**途中で車内人数がマイナス**」を検出（第4章） | 基準値案 R2（乗降カウント）の根拠と限界。※1997年の米国・カナダの調査 |
| **Zimmerman et al. (2011) Tiramisu**（CHI） | 全文 | 乗客が GPS 軌跡と**バスの満員度を報告**するシステムのフィールド実験。**28名・2週間（謝礼20ドル）＋1週間（無償）**。共有率は約56%で**期間中に低下**（目新しさの効果）。**学生は社会人より共有率が低い**。TAM3 評価は天井効果（p.6–9） | 最も近い先行事例。フィールド実験の規模感と苦労（ダイヤ改正・端末故障） |
| **Izard & Dehaene (2008)**（Cognition、数の見積もり） | 序論・実験1・実験2（p.1–13） | 実験1（5名）：点の数を**全員が大きく過小評価**。「30」と答えた時の実際の数は**58.6〜82.1個**（p.6–7）。実験2（24名）：「これは30個」と基準を1回見せると、実際の基準25/30/39個に応じて「30」の実数が**27.4/31.5/40.1個**になり、**影響は95〜100個の範囲まで全体に及ぶ**（p.10） | 「混雑時は数えられないのでは」への答え。**初期値や「目安」表示が回答をずらす**根拠。※点を一瞬見せる課題で、時間をかけて数えられる車内とは違う |
| **van Berkel et al. (2017)**（ACM CSUR、スマホ即時回答研究の総覧） | §3.1, §4.3〜4.6 | **期間の中央値14日、参加者数の中央値19名**。平均回答率69.6%。少人数ではベイズ統計を推奨。**10〜15分の個別説明で信頼関係を作る**。**高額謝礼がデータの質を下げた例**あり。抽選型謝礼は回答率が最低 | 参加者数の相場、謝礼なしでの募集の工夫、データ除外基準 |
| **Fryer & Nakao (2020)**（スマホの回答形式4種の比較） | 全文 | 81名・644回答。回答ごとに形式をランダム割当。**使ううちに形式間の時間差が急速に縮んだ＝学習効果**（p.19–20）。カウンターバランスしていないことを限界に挙げている | 「慣れちゃったらどうするのか」への答え（練習・カウンターバランス・試行番号の統制） |
| **Caine (2016)**（CHI論文の参加者数の実態） | 要旨・Results・考察 | CHI 2014 で**最も多い参加者数は12**。対面研究の平均18・中央値15。**被験者内実験の平均17**（被験者間は26）。一方で「**2条件・被験者内・17名は大きな効果でも検出力0.49**」（p.988） | 「10人で成り立つか」への正直な答え |
| **奈良交通「CI-CAのご利用方法」**（公式ページ） | 本文 | 多区間運賃制は乗降両方でタッチ、均一運賃は乗車時のみ。**NAIST方面（学園前・生駒地区）は運賃申告方式**で、駅→住宅地は駅から乗った人は降車時のみ、住宅地→駅は乗車時のみ | IC記録は乗降の片側しかなく現金客も含まない → **真値ではなく下限値** |

### 5.4 本文を読めていない・未確認のもの（**判断に使わないこと**）
| 情報 | 状態 | 対応 |
|---|---|---|
| Pi et al. (2018) 乗客が感じる混み具合と自動計数の実人数の関係（TRR） | アブストのみ（有料） | **クラス設計の議論に最重要**。大学図書館で本文入手 |
| Liu & Conrad (2019) スライダーの初期値が回答を引き寄せる | アブスト＋Fryer & Nakao 内の引用のみ | 本文入手 |
| Trick & Pylyshyn (1994) 数え上げは1人あたり約250〜350ms | 検索要約のみ | 本文を読むまで論文に書かない（計画書 §9 で「二次情報」と明記済み） |
| Tversky & Kahneman (1974) アンカリング効果の古典 | 未読 | 仕様書で引用しているので、使うなら本文を読む |
| TCRP Synthesis 77（2008年の改訂版） | 取得失敗（URL がHTMLだった） | 手作業計数の精度の数値があるか確認したい |
| 学園前駅〔北〕→NAIST 約25分・450円 | 検索要約（NAVITIME）のみ | 奈良交通「奈良バスなび」で確認 |

### 5.5 調査から引き出した考え方（計画に反映済み）
- **R1（同じ便・同じ瞬間に条件だけ変える）**なら真値が無くても条件間の差はUI由来と言える → 主張の柱
- **R2（ドアの乗降カウント）**は「車内を目で数える」のとは別物で、交通業界の標準的な方法。合計の一致で誤りを数値で示せる → **ただしユーザーは以前「実験者の計数」を却下している。R2 として改めて提案した段階で、採否は未決**
- **1便の中で混雑が変化する便**（例：夕方の住宅地→駅）を選べば、ラッシュに乗らなくても混雑の幅が取れる
- UIの初期値・目安表示は「基準」として働き、正しければ見積もりを改善、ずれていれば全体をずらしうる（Izard & Dehaene）

---

## 6. 教授との打ち合わせ（2026-09-12）の要点

詳細は `docs/log/2026-09-12.md`。
- **実施地**：3人などで乗ると不審に思われトラブルになりうる。**大学院経由で連携している奈良交通に頼むのが確実**。大阪は依頼がたらい回しになる恐れ。→ **奈良を仮定して計画**（ただし大阪か奈良かは正式には未決。奈良なら進学先研究室の確認が要り、研究室は海外出張中）
- **締切：実験は2026年11月中に終わらせる**
- 奈良は遠い。**謝礼なしで来てくれるか**（ユーザーの人望次第）。往復交通費とバス運賃は**ユーザーが用意**する必要があり、手続きが面倒だが明確にする
- **最大の弱点＝真値が分からない**。対外発表で攻撃されるのは目に見えている。「混雑時は正確に数えられないのでは」への答えも用意する（ラッシュ時に1人で乗って数えてみる**個人試行**。研究データには使わない）
- **評価の観点**（使いやすさか、正確な人数の取りやすさか）を決める
- **UIに設計思想（iPod のようなこだわり）が感じられない** → Claude がどういう基準で配置したかを説明させ、ユーザーが本を読んで改善を指示。何パターンか作って比較。院進後に「なぜこの形か」を説明できるように
- **進め方**：詰まったら教授に質問 or コミットして GitHub で共有。会話が長くなったら教授が要約。**AIの調査は本文PDFを読ませる**

---

## 7. 文書間の食い違い（決定が出たら揃えること）

| 箇所 | 食い違い | 状態 |
|---|---|---|
| `UI_STUDY_SPEC.md` §2 と `EXPERIMENT_PLAN.md` §2 | 仕様書は「実験者が同乗して数える案は採用しない」、計画書は「ドアの乗降カウント（R2）の採用を推奨」 | **R2 の採否待ち**。決まったら仕様書を直す |
| `UI_STUDY_SPEC.md` §4 と `EXPERIMENT_PLAN.md` §4 | 仕様書：10名・1人2〜3乗車・15〜18回答。計画書：12名・1日1回来訪・往復2乗車・12〜16回答 | **計画書が新しい**。教授の確認後に仕様書を合わせる |
| `UI_STUDY_SPEC.md` §3「大阪か奈良かは未定」と計画書「奈良を仮定」 | 矛盾ではない（奈良を仮定して計画、正式決定はまだ） | そのまま |
| 計画書 §3.1「実験者が各参加者の開始位置を指定」 | （解消済み） | ✅ 実装（`03e1a7a` / uiStudy-0.6）。実験バーの「参加者」番号で指定する |
| `DESIGN_PHILOSOPHY.md` §4 の比較パターン P0〜P3 | 本実験の4条件と両立させるか未決 | 教授と相談 |

---

## 8. 決まっていないこと（誰の判断待ちか）

| # | 事項 | Claude の推奨 | 判断者 |
|---|---|---|---|
| 1 | 評価の観点 | 回答の質が主、使いやすさが副 | ユーザー・教授 |
| 2 | 基準値の作り方 | R1＋R2、R3（IC記録）は奈良交通に依頼 | ユーザー・教授 |
| 3 | 本実験の条件（現行4条件か、P0〜P3 に差し替えか） | 現行4条件。P0〜P3 は研究室内の短時間テストで | 教授 |
| 4 | 設計思想の1文（甲：揺れる車内で片手3秒／乙：数えていない人に数えたふりをさせない／丙：答えた人が役立ったと分かる） | 甲×乙 | ユーザー・教授 |
| 5 | 実施地（奈良か大阪か）・路線・時間帯 | 奈良、平日15〜18時、1便で混雑が変化する方向。下見で決定 | ユーザー（奈良なら進学先研究室の確認後） |
| 6 | 費用の支出元・精算方法 | — | 事務・教授 |
| 7 | 研究倫理審査の要否 | リードタイムが長いので早めに確認 | 教授 |
| 8 | 教授と共有する GitHub リポジトリの作成 | 非公開リポジトリ＋教授を Collaborator | ユーザー（**作成は外部への操作なので必ず確認**） |
| 9 | 実験版アプリの公開方法（参加者のスマホから HTTPS で開ける必要。位置情報は HTTPS 必須） | — | ユーザー |
| 10 | 大学から乗車駅までの往復運賃（費用試算の A） | ユーザーの最寄り駅が分かれば計算 | ユーザー |
| 11 | ~~ステップ幅を ±1/±5 に戻すか~~ | **2026-09-17 決定：戻した**（`uiStudy-0.7.2` / `phaseA-1.7`）。ボタン幅 49.1/58.8/61.5px で全機種 HIG 44pt 達成。H2 の標的面積比は 1.95→1.40倍(375px)・2.53→1.82倍(320px)に縮小（**解消はしていない**＝限界として記述し、ボタン実寸をログに記録） | 決定済 |
| 12 | クラスの区切りの根拠（`DESIGN_PHILOSOPHY.md` B7） | 由来が確認できていない。先輩に照会して確定させる。`median` 条件のプリセット値が依存している | ユーザー・先輩 |
| 13 | 先輩に動作確認を依頼するか | 依頼するなら本番データへの混入を防ぐ段取り（練習モード or 事後除外手順）を先に決める | ユーザー |

---

## 9. 次にやること（優先順の案）

1. 上の §8 の 1〜4 を教授と決める（決まったら §7 の食い違いを解消）
2. Supabase で `ui_responses.sql` と `ui_sessions.sql` を実行（**ユーザーが Supabase の SQL Editor で実行**。実行するまでデータは端末にしか残らない）
3. ~~実験者が参加者ごとに条件の開始位置を指定できるようにする（計画書 §3.1）~~ → **完了**（`03e1a7a`, uiStudy-0.6）。残る判断＝練習回答の扱い（§4 の⚠️）
4. 実験版アプリの公開（§8-9）
5. バス停辞書を実施路線に絞る（奈良なら既存 1236件から、大阪なら P11 大阪府(27) から。**2010年データなので事業者公式で照合**）
6. 事後アンケートを Google Forms で作る（4条件のスクリーンショットを準備）
7. 分析スクリプト（JSON → pandas、混合効果モデル・ICC）
8. 下見 → パイロット（1〜2名）→ 本実験（10月下旬〜11月中旬）

---

## 10. ユーザーの方針・作業スタイル（厳守）

- **日本語**で対応。専門用語は簡単な言葉やたとえ話で説明する（「アンカリング効果って何？」と聞かれたことがある）
- 選択肢を並べるだけでなく**推奨を示す**。判断が要るものは「保留」として聞く
- **外部データは出典・整備主体・年次を必ず明示**（例：国土数値情報＝国土交通省・2010年）
- **文献は本文を読む**（§5.1）
- **位置・人数・バス停は任意。回答者に強制しない**
- **NAIST 提出アプリは原則触らない**。ユーザーが両方と言ったときだけ。**push は必ず許可を得てから**
- **UIの変更は原則に紐づける**。`DESIGN_PHILOSOPHY.md` §5 の変更台帳に根拠を書けない変更はしない（教授の「設計思想がない」という指摘への対応）
- **変更したら必ずコミット**（別アカウントと交代で作業しているため）。区切りごとに `docs/log/YYYY-MM-DD.md` に要点を残す
- **実験の前提を勝手に置かない**（ラボ実験を前提にして訂正された経緯あり）。分からなければ聞く

---

## 11. 技術メモ（環境・動作確認・落とし穴）

### 環境
- Windows 11。シェルは Git Bash と PowerShell。Python 3.13（`pypdf` あり）
- **無いもの**：`pdftoppm`（Read ツールで PDF 不可）、`node`、`gh` CLI
- git のグローバル設定が無い。コミットは `git -c user.name=taichi -c user.email=taichi1104.deters@gmail.com commit ...` の形で行ってきた

### 動作確認のやり方
- `研究\★ikenaga_san\.claude\launch.json` に `study-static`（`python -m http.server 8777`、`研究` フォルダを配信）がある。ブラウザで `http://localhost:8777/bus-ui-study/index.html`
- **ログインせずに人数画面を出す**（ブラウザのコンソールで）：
  ```js
  view='report'; user={id:'t',email:'t@id.local'}; render();
  pending={cid:'x',congestion_class:4,variant:{input:'numpad',count_init:'none'}}; renderCount();
  ```
- 画面サイズは **375×667（iPhone SE2/SE3）と 320×568（SE1）** で確認してきた。`.center` の `scrollHeight - clientHeight` がはみ出し量

### 落とし穴（実際に踏んだもの）
- **Bash ツールがバックスラッシュを潰す**：Python に Windows パスを渡すと `\n` が改行扱いになる。パスは `/` 区切りで書く
- **CSS クラス名 `.empty` は使用済み**（「まだ記録がありません」用、`padding:28px`）。人数欄の未入力状態は `.noval`
- ステッパー行の人数枠 `.count` に**高さを明示しない**（グリッドの stretch でボタンの高さに自動で揃える。明示するとずれる）
- localStorage のキー：`uistudy_sessions_v1`（未送信セッション）、`uistudy_seq_v1`（条件の通し番号）、`uistudy_offset_v1`（開始位置）、`uistudy_outbox_v1`（未送信回答）、`uistudy_posture_v1`（姿勢）、`uistudy_pid_v1`（参加者番号）、`uistudy_practice_v1`（練習モード）、`uistudy_pseq_v1`（練習カウンタ）、実験コード用のキー（`EXP_CODE_KEY`）。**実験の前に端末ごとに初期化すること**
- 版番号：実験版 `uiStudy-0.7`、NAIST 版 `phaseA-1.5`。変更したら上げる

### 変更履歴（bus-ui-study）
```
03e1a7a 09-13 実験者が参加者ごとに条件の開始位置を指定できるように（ラテン方格）
4d8d46d 09-13 別アカウントへの引継ぎ書 HANDOFF.md を追加
07d80dd 09-12 教授との打ち合わせを受けて計画を立て直し（奈良交通前提の大枠計画・文献・設計思想）
12ccd92 09-12 実験コード・着席/立席・t_first/t_count・回答⇔操作ログの結合キーを追加
c8d4bd6 09-10 研究計画を全面改訂(v1.0)し、事後アンケートを設計
7d6cde4 09-10 入力方式(ステッパー/テンキー)をUI条件に追加し、2要因4条件へ
54c583b 09-09 人数表示を±ボタンの行内へ移動し、常に枠で囲う
f78e968 09-08 人数入力: クラス制限を撤廃し±1/5/10ステッパーへ、小画面レスポンシブ対応
463bf6a 09-06 操作ログ計装とUI条件の自動割当を実装（UI研究の土台）
44a29c0 09-06 init: UI/ユーザビリティ研究用プロジェクト（NAIST提出アプリのコピーを土台）
```
