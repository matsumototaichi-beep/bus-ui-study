# 先輩（Bさん）への返信 文案

作成：2026-09-17 ／ 送信者：松本

対象は **NAIST アプリ**（`naist-bus-congestion`）。卒研（UI研究）の話は混ぜない。
方針：大げさにせず、修正した事実を述べる。質問は1つだけ。

---

```
Bさん

先日のフィードバック、対応したので報告します。

①入力範囲の制限
撤廃しました。どのラベルを選んでも0〜99人を入力できます。
送信時のチェックもDB側の制約も外したので、ラベルと人数が食い違っていてもそのまま送れます。
表示も「入力範囲」→「目安」に変えました。
人数の見出しは「自分を含めた正確な乗客数（任意）」にしています。
自分を数に入れるか、運転士を含めるかで解釈が割れると思ったので、
そこが決まるようにしました。

②±の刻み
±1/±5 の2段階にしました。
一度±1/5/10の3つを並べたのですが、ボタンが横に7つ並ぶぶん1個が細くなって、
iPhone SEで35px、SE2/SE3で42pxと、Appleのガイドライン（44pt）を下回っていました。
2段階なら49px/59pxで収まります。ご指摘のとおりでした。
比較してから決めると言っていましたが、実測でこの差が出たので2段階にしています。

③iPhone SEでの窮屈さ
ボタンのはみ出しを直しました。
あわせて点検し直したところ、管理者アカウント（Bさんと共有しているID:adminの方）で
ログインすると「データ一覧」ボタンが増えるぶんヘッダーが折り返して、
小さい画面では送信ボタンが画面外に出ていました。そこも直しています。
送信/スキップは画面下端に固定したので、横向きやキーボード表示中でも見えます。

ほかに、人数を入力している途中でヘッダーの「報告」やENを押すと入力値が消えて、
中途半端なデータが入る不具合が見つかったので直しました。
どの版か分かるように、画面下に版番号を出しています（今 phaseA-1.10）。

1点うかがいたいのですが、前回の
「異なる混雑度ラベルでも，ある程度の範囲は同じ人数の入力は許容してもいい」は、
(a) 入力自体を通す（＝今回の対応）という意味でしょうか、それとも
(b) 目安の数値を隣のクラスと重ねて見せる（例：クラス3を「15〜27人程度」）
という意味でしょうか。(b)ならまだ手を付けていないので、教えていただけると助かります。

もし触っていただく場合、いま練習モードが無いので試した分も本番データに入ります。
そこは私の方で後から除外するので、気にせず触ってください。

よろしくお願いします。
```

---

## 送信前チェック

- [x] 版番号 `phaseA-1.10` はリモート反映済み（`4ea2e89`、2026-09-17 push）
- [ ] 「ID:admin」の呼び方が先輩に通じるか
- [ ] 数字（35px / 42px / 44pt / 49px / 59px）をそのまま出すか、落として「細くなりすぎていた」で済ませるか

## この返信に入れなかったもの

| | 理由 |
|---|---|
| 値域の根拠の説明 | 記憶どおり（座席数と最大乗車数から概算）なので訂正不要。ただし**計算の内訳が未記録**なのは別途対応（`DESIGN_PHILOSOPHY.md` B7） |
| ±1/5 対 ±1/5/10 の比較そのもの | ±1/5 を採用したので不要。卒研では**実施しない**と決定（2026-09-21） → **卒研側で管理** |
| `rich_labeled_bus_data.csv` の利用可否 | 卒研の判断材料の話 → **卒研側で管理** |
| H2（stepper vs numpad）の標的サイズ交絡 | 卒研の内部事情。先輩のフィードバックへの回答ではない |
| 文字拡大（`user-scalable=no`）・横向きの横幅活用 | 聞かれていない。優先度が低く、話が長くなる |
| 9/9 のコミット（人数表示を±の行内へ移動） | **先輩の指摘ではない**。混ぜると言っていないことを言ったことになる |
