# 2026-09-18 作業ログ（阪急バス・卒研側）

方針：卒研アプリの実験環境（データ保存・ホスティング）をNAIST版と同じ構成で整える。

## 1. 現状把握

- Supabase接続先（URL/キー）はNAISTアプリと**同じプロジェクト**を使う設計だった（テーブル名で分離）。
  ただし卒研用テーブル（`ui_responses`/`ui_sessions`）はSupabase上に未作成で、送信しても保存されない状態
- ホスティングも未設定（`bus-ui-study` は意図的にリモート未設定・ローカルのみ）
- NAISTアプリは GitHub Pages（public リポジトリ）で公開：
  https://matsumototaichi-beep.github.io/naist-bus-congestion/

## 2. GitHub Pagesで公開

NAIST版と同じ方式（GitHub Pages）を採用。松本の承認のうえ、新規 public リポジトリを作成：

- リポジトリ：`matsumototaichi-beep/bus-ui-study`（public、`master`ブランチ）
- 公開URL：**https://matsumototaichi-beep.github.io/bus-ui-study/**（HTTPS、動作確認済み）

※ リポジトリ作成・push は Claude Code の Auto Mode 安全装置に「外部公開操作」として一度拒否されたため、
松本が手元のターミナルで `gh repo create ... --push` 等を実行した。

### 副次的に見つかった不具合：ログイン画面が「NAIST共同研究」表記のまま

コピー元の文言が残っていた（`index.html` 日英とも）。卒研はNAISTとの共同研究ではないため、
参加者向け説明として誤り。「卒業研究です」に修正して再push済み（反映確認済み）。

## 3. Supabaseにテーブル作成

`ui_responses.sql` → `ui_sessions.sql` → `ui_report_feed.sql` の順に、松本がSupabase SQL Editorで実行。

- 実行後、REST API（匿名キー）で存在確認：`ui_responses`・`ui_sessions` は200 OK（RLSにより空配列）、
  `ui_report_feed` は `permission denied`（ビューの権限設計どおりで異常ではない）
- Table Editor上での表示更新が遅れて「見えない」と一時混乱したが、リロードで解消
- **実機テスト送信 → Supabase SQL Editorで `ui_responses` に1行増えていることを確認**。
  データ保存の仕組みが完成した

## 4. 現状（環境面）

| 項目 | 状態 |
|---|---|
| 公開URL | https://matsumototaichi-beep.github.io/bus-ui-study/ |
| アプリ版 | `uiStudy-0.7.4` |
| バス停辞書 | 阪急バス735件（`HANKYU_STOPS`） |
| Supabase | `ui_responses`/`ui_sessions`/`ui_report_feed` 作成済み、保存確認済み |

## 5. ログイン画面の「実験コード」「参加者」について、松本から3点の指摘

1. **参加者番号・実験コードをアプリに入ってから設定するのは面倒**。ログインの時点で済ませたい
2. **クラスによって入力するUIの形（テンキー/ステッパー）が違う**のは意図した実装か？→ はい
   （`EXPERIMENT_PLAN.md` §3.1 のラテン方格。クラスとは無関係で、回答の順番で4条件を回している）
3. さらに松本の意向：**入力方式ごとにアプリ（URL）を分けて、実験者が配布するアプリを固定したい**

3点目は影響が大きいので、先に指摘した：入力方式をアプリで固定すると、①（同じ人が両方の入力方式を
経験する＝被験者内比較）が失われる。3つ確認した結果：

- ①が失われることは**受け入れる**（現状の考え方でOK、ただし下記のバグは直してほしい）
- 何本のアプリに分けるかは**まだ決めていない**
- 配布方針：**全参加者に全アプリを配布し、実験番号で各参加者が使うアプリを固定。
  同じ便・同じ時間の参加者は同じアプリを使う** → これは②（同じ瞬間の参加者間の条件反転）も
  失うことを意味する（`UI_STUDY_SPEC.md` §11 に追記済み）。実装は**本数が決まるまで保留**

## 6. 今回実装した3点

1. **バグ修正**：条件（variant）割当のコマ消費を「クラスタップ時」→「送信/スキップ確定時」に変更。
   `nextVariant()` を `peekVariant()`（覗くだけ）/`commitVariant()`（実際に1つ進める）に分離。
   クラスをタップして「戻る」でキャンセルしても、ラテン方格の1コマを消費しなくなった。
   JSコンソールで peek/commit の単体動作を確認済み（`DESIGN_PHILOSOPHY.md` 変更台帳参照）
2. **参加者番号を新規登録フォームに統合**（`fPid` 欄）。登録成功時に `setDaily(PID_KEY,...)` と
   ラテン方格の通し番号リセットを行う
3. **実験コードの確認ゲートを新設**（`renderPrepGate()`）。参加者番号または実験コードが未設定なら、
   報告画面より前に必ず表示される。管理者アカウントはゲート対象外（動作確認用）。
   ブラウザで入力→保存→報告画面遷移を確認済み

`uiStudy-0.7.4` としてGitHub Pagesに反映済み。

## 6.5 さらに指摘：ログアウト→再ログインで着席/立席が残る（`uiStudy-0.7.5`）

松本から2点：
1. **バグ**：ログアウトして再ログインしても、着席/立席（`POSTURE_KEY`）が前の状態のまま残っていた
2. **質問**：ブラウザを閉じた場合もログアウト状態になるのか？

対応・回答：
- ログアウトボタンのハンドラで `POSTURE_KEY`・`PRACTICE_KEY`（練習モード）を明示的にクリアするよう修正。
  実験コード・参加者番号は「同じ参加者が再ログインするだけなら変える必要がない」「参加者が変わる場合は
  新規登録でPIDが入り直る」ため、あえて残した
- ブラウザを閉じただけではログアウトにならない。Supabaseクライアントは `persistSession:true` /
  `autoRefreshToken:true` で初期化しており、セッションはブラウザのlocalStorageに保存され続ける。
  次にリンクを開いたときは自動的にログイン済み状態から始まり、実験コード等が未設定でなければ
  すぐ回答画面（報告画面）に戻る。ログアウトになるのは、明示的にログアウトボタンを押した場合、
  ブラウザのサイトデータを消去した場合、別の端末/ブラウザで開いた場合など
- ブラウザで `setDaily`→ログアウトボタンクリック→ `null` になることを確認

## 7. 次にやること

- 下見（候補A：JR吹田駅⇔桃山台駅）の結果待ち
- 入力方式のアプリ分割：本数と初期値要因の扱いを松本と相談して決める
- `uiStudy-0.8`（停留所ID割当表。改修凍結は10月上旬）
- ログ計装・UI条件の割当・分析基盤は `README.md`「これからやること」参照
- 系統確定後、管轄営業所へ連絡（承諾条件④）
